# 実行時に生成される作業領域。プレイブックの内容ではないので追跡しない。
.r4-coordinator-stage/
.r5-isolation-probe/
.r5-sbx-capability-probe/
error.log

# 調査・修正候補の一時作業。正本の複製を含みうるので、作業が終わったら中身ごと削除する。
# 除外は誤commit対策であって、整理の代わりにはならない（2026-09-19）。
.tmp/

# 後片付け処理を持つが、強制終了時に残りうる生成物（2026-09-19 に実測で洗い出した）。
# 生成先はリポジトリ内であることを検証する実装があるため、生成先だけを外へ移さない。
.r5-ambient-provenance-*/
.checkpoint-diff-e2e-*/

# エージェント個別の設定。プレイブック本体とは別に管理する。
.claude/
.codex/

# 案件固有のパイロット実行記録。案件のローカルパス・pair ID・承認 hash を直接持つため、
# 公開リポジトリに置かずローカルにだけ残す（2026-09-11。WORKFLOW.md「Figma差異指摘の保存先」）。
tools/r4-*
tools/r5-baseline-reissue-3-candidate-*
tools/r5-launch-baseline-seq1.mjs
tools/r5-prepare-baseline-seq1-launch-records.mjs
research/p3/p3-role-return.e2e.mjs
