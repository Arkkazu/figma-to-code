#!/usr/bin/env node
// Isolated regression coverage for P-3 comparison contract v13.
import { execFileSync } from "node:child_process";
import { createHash } from "node:crypto";
import { copyFileSync, existsSync, mkdirSync, mkdtempSync, readFileSync, rmSync, writeFileSync } from "node:fs";
import { createServer } from "node:http";
import { tmpdir } from "node:os";
import { dirname, join, resolve } from "node:path";
import { fileURLToPath } from "node:url";

// 複製元は cwd ではなく、このE2E自身の隣から取る。
// 旧実装は resolve("templates/verify/...") と cwd 相対で書いており、正本リポジトリでしか
// 通らなかった。このE2Eは配布物であり、案件では MyBrain/verify/ に置かれる。
// 実測（2026-08-29、rpa-technologies-theme）: 配布後に実行すると
// `ENOENT: copyfile <案件>/templates/verify/fidelity-benchmark.mjs` で落ちた。
// 同じ欠陥を figma-gate.e2e.mjs でも直している。正本リポジトリ内でだけ回すE2Eは、
// 配布先で動くことを証明しない。
const verifyDirectory = dirname(fileURLToPath(import.meta.url));
const sourceScript = resolve(verifyDirectory, "fidelity-benchmark.mjs");
const sourceProvider = resolve(verifyDirectory, "p3-page-provider.mjs");
const script = "MyBrain/verify/fidelity-benchmark.mjs";
const providerPort = await new Promise((resolvePort, rejectPort) => { const server = createServer(); server.once("error", rejectPort); server.listen({ host: "127.0.0.1", port: 0, exclusive: true }, () => { const address = server.address(); const port = typeof address === "object" && address ? address.port : 0; server.close((error) => error ? rejectPort(error) : resolvePort(port)); }); });
const providerUrl = `http://127.0.0.1:${providerPort}/p3`;
const names = ["manifest", "components", "mapping", "component-decision", "node-evidence", "layer-evidence", "accessibility", "motion", "axe-source"];
const evaluators = ["fidelityBenchmark", "figmaGate", "gateBrowserBatch", "verifyLayout", "checkpointCapture", "checkpointDiff", "cdpBrowser", "accessibilityVerify", "motionVerify", "lintUnits", "loopLearn", "loopLearningPolicy"];
const evaluatorPaths = Object.freeze({ fidelityBenchmark: "MyBrain/verify/fidelity-benchmark.mjs", figmaGate: "MyBrain/verify/figma-gate.mjs", gateBrowserBatch: "MyBrain/verify/gate-browser-batch.mjs", verifyLayout: "MyBrain/verify/verify-layout.mjs", checkpointCapture: "MyBrain/verify/checkpoint-capture.mjs", checkpointDiff: "MyBrain/verify/checkpoint-diff.mjs", cdpBrowser: "MyBrain/verify/cdp-browser.mjs", accessibilityVerify: "MyBrain/verify/accessibility-verify.mjs", motionVerify: "MyBrain/verify/motion-verify.mjs", lintUnits: "MyBrain/verify/lint-units.mjs", loopLearn: "MyBrain/verify/loop-learn.mjs", loopLearningPolicy: "MyBrain/verify/loop-learning-policy.json" });
const evaluatorBaselinePath = "MyBrain/verify/e2e-frozen/p3-evaluator-baseline.json";
let baselineWorktreeRoot = null;
let currentWorktreeRoot = null;
const sha = (root, pathname) => createHash("sha256").update(readFileSync(join(root, pathname))).digest("hex");
const stable = (value) => Array.isArray(value) ? value.map(stable) : value && typeof value === "object" ? Object.fromEntries(Object.keys(value).sort().map((key) => [key, stable(value[key])])) : value;
const stableHash = (value) => createHash("sha256").update(JSON.stringify(stable(value))).digest("hex");
const canonicalP3Path = (pathname) => { const value = resolve(pathname).replace(/\\/g, "/"); return process.platform === "win32" || process.platform === "darwin" ? value.toLowerCase() : value; };
const trace = (...values) => { if (process.env.FIGMA_P3_E2E_TRACE === "1") process.stderr.write(`[p3-e2e] ${values.join(" ")}\n`); };
const evidence = (root, pathname) => ({ path: pathname, sha256: sha(root, pathname) });
const put = (root, pathname, value) => { const target = join(root, pathname); mkdirSync(dirname(target), { recursive: true }); writeFileSync(target, typeof value === "string" || Buffer.isBuffer(value) ? value : `${JSON.stringify(value, null, 2)}\n`, "utf8"); };
const read = (root, pathname) => JSON.parse(readFileSync(join(root, pathname), "utf8"));
const git = (root, ...args) => execFileSync("git", args, { cwd: root, encoding: "utf8" }).trim();
const evaluatorRoots = (root) => Object.fromEntries(evaluators.map((name) => [name, evidence(root, evaluatorPaths[name])]));
const runtimeArtifacts = (root) => [...new Set([...readFileSync(join(root, evaluatorPaths.figmaGate), "utf8").matchAll(/["'](MyBrain\/verify\/[A-Za-z0-9._-]+\.(?:mjs|json))["']/g)].map((match) => match[1]))].sort();
function evaluatorExecutionBundle(root) {
  const cjsEntry = readFileSync(join(root, "node_modules/e2e-cjs/index.cjs"), "utf8");
  const primaryCjsHelper = cjsEntry.includes("helper.cjs") ? ["node_modules/e2e-cjs/helper.cjs"] : [];
  const optionalCjsPaths = ["node_modules/e2e-cjs/helper-plain.cjs", "node_modules/e2e-cjs/helper-module.cjs", "node_modules/e2e-cjs/helper-resolve.cjs"].filter((pathname) => existsSync(join(root, pathname)));
  const cjsUsesConditional = cjsEntry.includes("e2e-conditional");
  const conditionalCjsPaths = cjsUsesConditional ? ["node_modules/e2e-conditional/require.cjs", "node_modules/e2e-conditional/require-helper.cjs"] : [];
  const closurePaths = [...new Set([...Object.values(evaluatorPaths).filter((pathname) => pathname.endsWith(".mjs")), "MyBrain/verify/p3-page-provider.mjs", "MyBrain/verify/gate-helper.mjs", "node_modules/e2e-bare/index.js", "node_modules/e2e-bare/helper.js", "node_modules/e2e-cjs/index.cjs", ...primaryCjsHelper, ...optionalCjsPaths, "node_modules/e2e-conditional/import.js", "node_modules/e2e-conditional/import-helper.js", ...conditionalCjsPaths])].sort();
  const packages = [
    { fromPath: "MyBrain/verify/figma-gate.mjs", mode: "import", specifier: "e2e-bare", resolvedPath: "node_modules/e2e-bare/index.js", resolvedSha256: sha(root, "node_modules/e2e-bare/index.js"), packageJsonPath: "node_modules/e2e-bare/package.json", packageJsonSha256: sha(root, "node_modules/e2e-bare/package.json"), lockKey: "node_modules/e2e-bare", version: "1.0.0" },
    { fromPath: "MyBrain/verify/figma-gate.mjs", mode: "import", specifier: "e2e-cjs", resolvedPath: "node_modules/e2e-cjs/index.cjs", resolvedSha256: sha(root, "node_modules/e2e-cjs/index.cjs"), packageJsonPath: "node_modules/e2e-cjs/package.json", packageJsonSha256: sha(root, "node_modules/e2e-cjs/package.json"), lockKey: "node_modules/e2e-cjs", version: "1.0.0" },
    { fromPath: "MyBrain/verify/figma-gate.mjs", mode: "import", specifier: "e2e-conditional", resolvedPath: "node_modules/e2e-conditional/import.js", resolvedSha256: sha(root, "node_modules/e2e-conditional/import.js"), packageJsonPath: "node_modules/e2e-conditional/package.json", packageJsonSha256: sha(root, "node_modules/e2e-conditional/package.json"), lockKey: "node_modules/e2e-conditional", version: "1.0.0" },
  ];
  if (cjsUsesConditional) packages.push({ fromPath: "node_modules/e2e-cjs/index.cjs", mode: "require", specifier: "e2e-conditional", resolvedPath: "node_modules/e2e-conditional/require.cjs", resolvedSha256: sha(root, "node_modules/e2e-conditional/require.cjs"), packageJsonPath: "node_modules/e2e-conditional/package.json", packageJsonSha256: sha(root, "node_modules/e2e-conditional/package.json"), lockKey: "node_modules/e2e-conditional", version: "1.0.0" });
  return {
    version: 1,
    canonicalRoots: evaluators.map((key) => ({ key, ...evaluatorRoots(root)[key] })),
    gateRuntimeArtifacts: runtimeArtifacts(root).map((pathname) => ({ key: evaluators.find((candidate) => evaluatorPaths[candidate] === pathname), ...evaluatorRoots(root)[evaluators.find((candidate) => evaluatorPaths[candidate] === pathname)] })),
    staticImportClosure: closurePaths.map((pathname) => evidence(root, pathname)),
    packageLock: evidence(root, "package-lock.json"),
    resolvedBarePackages: packages.sort((a, b) => a.fromPath.localeCompare(b.fromPath) || a.specifier.localeCompare(b.specifier) || a.mode.localeCompare(b.mode) || a.resolvedPath.localeCompare(b.resolvedPath)),
  };
}
function writeEvaluatorBaseline(root) {
  put(root, evaluatorBaselinePath, { version: 2, status: "approved", ownerApproved: true, approvedAt: "2026-08-08T00:00:00.000Z", basis: "Owner-approved project evaluator baseline; the listed path and resulting SHA-256 are the approved project-specific measurement artifacts.", artifacts: Object.entries(evaluatorRoots(root)).map(([key, artifact]) => ({ key, ...artifact })), executionBundleSha256: stableHash(evaluatorExecutionBundle(root)) });
  return evidence(root, evaluatorBaselinePath);
}
const activeContractPaths = new Map();
const run = (root, ...args) => { trace("run:start", args.join(" ")); const output = execFileSync(process.execPath, [script, ...args], { cwd: root, encoding: "utf8" }); trace("run:pass", args.join(" ")); if (args[0] === "pair-preflight") activeContractPaths.set(root, args[1]); return output; };
function fails(root, expected, ...args) { trace("fail:start", args.join(" ")); try { execFileSync(process.execPath, [script, ...args], { cwd: root, encoding: "utf8", stdio: "pipe" }); } catch (error) { const output = `${error.stdout || ""}${error.stderr || ""}`; if (output.includes(expected)) { trace("fail:pass", args.join(" ")); return; } throw new Error(`${expected} not found: ${output}`); } throw new Error(`expected fail: ${args.join(" ")}`); }
function failsWithEnv(root, expected, environment, ...args) {
  try { execFileSync(process.execPath, [script, ...args], { cwd: root, encoding: "utf8", stdio: "pipe", env: { ...process.env, ...environment } }); }
  catch (error) { const output = `${error.stdout || ""}${error.stderr || ""}`; if (output.includes(expected)) return; throw new Error(`${expected} not found: ${output}`); }
  throw new Error(`expected fail: ${args.join(" ")}`);
}
function failsWithNodeArgs(root, expected, nodeArgs, ...args) {
  try { execFileSync(process.execPath, [...nodeArgs, script, ...args], { cwd: root, encoding: "utf8", stdio: "pipe" }); }
  catch (error) { const output = `${error.stdout || ""}${error.stderr || ""}`; if (output.includes(expected)) return; throw new Error(`${expected} not found: ${output}`); }
  throw new Error(`expected fail: ${args.join(" ")}`);
}
function ledgerRecords(root) { return readFileSync(join(root, ".git", "figma-p3-comparison-ledger.jsonl"), "utf8").split(/\r?\n/).filter(Boolean).map((line) => JSON.parse(line)); }
function assertAborted(root, pairId, label) { if (!ledgerRecords(root).some((record) => record.pairId === pairId && record.kind === "aborted")) throw new Error(`${label} did not write an aborted ledger record for ${pairId}`); }
function pairLockPath(root, pairId) { return join(root, ".git", "figma-p3-comparison-pair-locks", createHash("sha256").update(pairId).digest("hex") + ".json"); }
function assertNotReserved(root, pairId, label) {
  if (existsSync(pairLockPath(root, pairId))) throw new Error(`${label} created a fixed pair lock before rejecting a draft authority`);
  const ledgerPath = join(root, ".git", "figma-p3-comparison-ledger.jsonl");
  if (existsSync(ledgerPath) && ledgerRecords(root).some((record) => record.pairId === pairId)) throw new Error(`${label} wrote a pair ledger record before rejecting a draft authority`);
}
function assertReadinessDidNotCreateLifecycleArtifacts(root, pairId, label) {
  const paths = [
    join(root, ".git", "figma-p3-comparison-ledger.jsonl"),
    pairLockPath(root, pairId),
    join(root, ".figma-gate", "active.json"),
  ];
  for (const pathname of paths) if (existsSync(pathname)) throw new Error(`${label} created a lifecycle artifact: ${pathname}`);
}
function readinessLifecycleSnapshot(root, pairId) {
  const paths = [
    join(root, ".git", "figma-p3-comparison-ledger.jsonl"),
    pairLockPath(root, pairId),
    join(root, ".figma-gate", "active.json"),
  ];
  return paths.map((pathname) => ({ pathname, bytes: existsSync(pathname) ? readFileSync(pathname).toString("base64") : null }));
}
function assertReadinessDidNotMutateLifecycle(root, pairId, before, label) {
  const after = readinessLifecycleSnapshot(root, pairId);
  if (JSON.stringify(after) !== JSON.stringify(before)) throw new Error(`${label} changed a ledger, pair lock, or gate state`);
}
function assertPairCloseDidNotReachGateClose(root, comparisonPath, activeBefore, closeBefore, closeInvocationBefore, label) {
  const activePath = join(root, ".figma-gate", "active.json");
  if (!existsSync(activePath)) throw new Error(`${label} removed the active preflight state`);
  const activeAfter = readFileSync(activePath, "utf8");
  if (activeAfter !== activeBefore || read(root, ".figma-gate/active.json").phase !== "preflight") throw new Error(`${label} reached figma-gate close instead of stopping at a P-3 close precondition`);
  const closePath = join(root, "MyBrain/verify/e2e-out/close.json");
  const closeAfter = existsSync(closePath) ? readFileSync(closePath).toString("base64") : null;
  if (closeAfter !== closeBefore) throw new Error(`${label} wrote or changed a figma-gate close report`);
  const invocationPath = join(root, "MyBrain/verify/e2e-out/figma-gate-close-invocation.json");
  const closeInvocationAfter = existsSync(invocationPath) ? readFileSync(invocationPath).toString("base64") : null;
  if (closeInvocationAfter !== closeInvocationBefore) throw new Error(`${label} invoked figma-gate close before rejecting the P-3 close precondition`);
  const receiptPath = read(root, comparisonPath)?.run?.pageProviderReceiptPath;
  if (typeof receiptPath !== "string" || !receiptPath) throw new Error(`${label} has no provider receipt path`);
  if (existsSync(join(root, receiptPath))) throw new Error(`${label} wrote a provider receipt before rejecting the P-3 close precondition`);
}
function inputs(root) { return { manifest: evidence(root, "MyBrain/verify/e2e-frozen/manifest.json"), spec: evidence(root, "MyBrain/verify/e2e-frozen/spec.json"), components: evidence(root, "MyBrain/verify/e2e-frozen/components.json"), mapping: evidence(root, "MyBrain/verify/e2e-frozen/mapping.json"), nodeMap: evidence(root, "MyBrain/verify/e2e-frozen/nodemap.json"), componentDecision: evidence(root, "MyBrain/verify/e2e-frozen/component-decision.json"), nodeEvidence: evidence(root, "MyBrain/verify/e2e-frozen/node-evidence.json"), layerEvidence: evidence(root, "MyBrain/verify/e2e-frozen/layer-evidence.json"), accessibility: evidence(root, "MyBrain/verify/e2e-frozen/accessibility.json"), motion: evidence(root, "MyBrain/verify/e2e-frozen/motion.json"), axeSource: evidence(root, "MyBrain/verify/e2e-frozen/axe-source.json") }; }
function fixture(root) {
  mkdirSync(join(root, "MyBrain/verify"), { recursive: true });
  copyFileSync(sourceScript, join(root, evaluatorPaths.fidelityBenchmark));
  copyFileSync(sourceProvider, join(root, "MyBrain/verify/p3-page-provider.mjs"));
  put(root, ".gitignore", "MyBrain/verify/e2e-frozen/p3-source-snapshot.tar\nMyBrain/verify/e2e-frozen/p3-pre-implementation.json\nMyBrain/verify/e2e-frozen/p3-owner-decision.json\nMyBrain/verify/e2e-frozen/p3-owner-decision-*.json\nMyBrain/verify/e2e-frozen/p3-evaluator-baseline.json\nMyBrain/verify/e2e-frozen/p3-current-change-*.json\nMyBrain/verify/e2e-frozen/p3-clean-room-*.json\n");
  put(root, "MyBrain/verify/e2e-frozen/nodemap.json", { version: 2, schema: "scoped-roots/v1", figma: { fileKey: "figma-e2e", canonicalRootNodeId: "1:2" } });
  put(root, "MyBrain/verify/e2e-frozen/metadata.json", { fileKey: "figma-e2e", rootNodeId: "1:2" }); put(root, "MyBrain/verify/e2e-frozen/design.json", { design: true }); put(root, "MyBrain/verify/e2e-frozen/screenshot.png", "png"); put(root, "MyBrain/verify/e2e-frozen/spec.json", { spec: true });
  put(root, "MyBrain/verify/e2e-frozen/page.json", { version: 1, sections: [{ sectionId: "hero", role: "target", componentIds: ["hero", "cards"] }] });
  put(root, "MyBrain/verify/e2e-frozen/thresholds.json", { threshold: 1 });
  put(root, "MyBrain/verify/e2e-frozen/components.json", { components: [{ elementId: "hero", selector: ".p3-hero", figmaNodeId: "1:3", sectionId: "hero" }, { elementId: "cards", selector: ".p3-cards", figmaNodeId: "1:4", sectionId: "hero" }] });
  put(root, "MyBrain/verify/e2e-frozen/component-decision.json", { version: 1, decisions: [{ elementId: "hero", figmaNodeId: "1:3", codePath: "src/main.html" }, { elementId: "cards", figmaNodeId: "1:4", codePath: "src/main.html" }] });
  put(root, "MyBrain/verify/e2e-frozen/manifest.json", { id: "p3-manifest", scope: { verifyUrl: providerUrl, checkpointPlan: ["hero", "cards"], changeTargets: ["src/main.html"], pageCoveragePath: "MyBrain/verify/e2e-frozen/page.json" } });
  for (const name of names.filter((name) => !["manifest", "components", "component-decision"].includes(name))) put(root, `MyBrain/verify/e2e-frozen/${name}.json`, { name });
  put(root, "package-lock.json", { name: "p3-e2e", lockfileVersion: 3, packages: { "node_modules/e2e-bare": { version: "1.0.0" }, "node_modules/e2e-cjs": { version: "1.0.0" }, "node_modules/e2e-conditional": { version: "1.0.0" } } });
  put(root, "node_modules/e2e-bare/package.json", { name: "e2e-bare", version: "1.0.0", type: "module", exports: "./index.js" }); put(root, "node_modules/e2e-bare/helper.js", "export const helper = true;\n"); put(root, "node_modules/e2e-bare/index.js", "import { helper } from \"./helper.js\"; export const bare = helper;\n");
  put(root, "node_modules/e2e-conditional/package.json", { name: "e2e-conditional", version: "1.0.0", type: "module", exports: { import: "./import.js", require: "./require.cjs" } }); put(root, "node_modules/e2e-conditional/import-helper.js", "export const importedHelper = true;\n"); put(root, "node_modules/e2e-conditional/import.js", "import { importedHelper } from \"./import-helper.js\"; export const imported = importedHelper;\n"); put(root, "node_modules/e2e-conditional/require-helper.cjs", "module.exports = true;\n"); put(root, "node_modules/e2e-conditional/require.cjs", "const requiredHelper = require(\"./require-helper.cjs\"); module.exports = { required: requiredHelper };\n");
  put(root, "node_modules/e2e-cjs/package.json", { name: "e2e-cjs", version: "1.0.0", main: "./index.cjs" }); put(root, "node_modules/e2e-cjs/helper.cjs", "module.exports = true;\n"); put(root, "node_modules/e2e-cjs/index.cjs", "const helper = require(\"./helper.cjs\"); const conditional = require(\"e2e-conditional\"); const assertOk = require(\"assert\").ok; module.exports = Boolean(helper && conditional.required && assertOk);\n");
  put(root, "MyBrain/verify/gate-helper.mjs", "export const helper = true;\n");
  // This tracked but unrelated page exercises the rule that P-3 may measure
  // only the frozen implementation outputs, never an arbitrary source-side
  // static page that happens to resemble the intended screen.
  put(root, "static-decoy/index.html", "<!doctype html><title>unrelated static page</title>\n");
  put(root, "MyBrain/verify/figma-gate.mjs", `import { helper } from "./gate-helper.mjs"; import { bare } from "e2e-bare"; import cjs from "e2e-cjs"; import { imported } from "e2e-conditional"; import { createHash, randomUUID } from "node:crypto"; import { execFileSync } from "node:child_process"; import { mkdirSync, readFileSync, writeFileSync } from "node:fs"; import { resolve } from "node:path"; const runtimeArtifacts = ["MyBrain/verify/lint-units.mjs", "MyBrain/verify/loop-learn.mjs", "MyBrain/verify/loop-learning-policy.json"]; if (process.argv[2] !== "preflight" || !helper || !bare || !cjs || !imported || !runtimeArtifacts.length) process.exit(2); const root = process.cwd(), h = p => createHash("sha256").update(readFileSync(resolve(root,p))).digest("hex"), g = x => execFileSync("git",x,{cwd:root,encoding:"utf8"}).trim(), vals = {}; for (const n of ${JSON.stringify(names)}) vals[n] = h("MyBrain/verify/e2e-frozen/"+n+".json"); const state = { version:2, phase:"preflight", repository:root, runtime:{entryPath:resolve(root,"MyBrain/verify/figma-gate.mjs"),entrySha256:h("MyBrain/verify/figma-gate.mjs")}, git:{worktreeRoot:root,commit:g(["rev-parse","HEAD"]),tree:g(["rev-parse","HEAD^{tree}"])}, manifestId:"p3-manifest", preflightId:randomUUID(), preflightAt:"2026-08-07T00:00:00.000Z", preEdit:{dirtyPaths:[],changeTargetStatus:{"src/main.css":"clean"}}, manifestSha256:vals.manifest,specSha256:h("MyBrain/verify/e2e-frozen/spec.json"),componentsSha256:vals.components,mappingSha256:vals.mapping,nodeMapSha256:h("MyBrain/verify/e2e-frozen/nodemap.json"),componentDecisionSha256:vals["component-decision"],nodeEvidenceSha256:vals["node-evidence"],layerEvidenceSha256:vals["layer-evidence"],accessibilitySha256:vals.accessibility,motionSha256:vals.motion,axeSourceSha256:vals["axe-source"],changeTargets:["src/main.css"],benchmark:{plan:["hero","cards"],attempts:[]},learningMetrics:{directViewportRuns:{hero:{pc:1},cards:{pc:1}}}}; mkdirSync(resolve(root,".figma-gate"),{recursive:true}); writeFileSync(resolve(root,".figma-gate/active.json"),JSON.stringify(state,null,2));`);
  for (const [name, pathname] of Object.entries(evaluatorPaths)) if (name !== "figmaGate" && name !== "fidelityBenchmark") put(root, pathname, name === "loopLearningPolicy" ? { version: 1, policy: "e2e" } : `// ${name}\n`);
  put(root, "MyBrain/verify/figma-gate.mjs", String.raw`
import { createHash, randomUUID } from "node:crypto";
import { execFileSync } from "node:child_process";
import { existsSync, mkdirSync, readFileSync, writeFileSync } from "node:fs";
import { get } from "node:http";
import { resolve } from "node:path";
import { helper } from "./gate-helper.mjs";
import { bare } from "e2e-bare";
import cjs from "e2e-cjs";
import { imported } from "e2e-conditional";
const root = process.cwd();
const command = process.argv[2];
const manifestPath = process.argv[3];
const implementationActor = process.argv[4] === "--implementation-actor" ? process.argv[5] : null;
const implementationContextId = process.argv[6] === "--implementation-context-id" ? process.argv[7] : null;
const h = (pathname) => createHash("sha256").update(readFileSync(resolve(root, pathname))).digest("hex");
const textHash = (value) => createHash("sha256").update(value).digest("hex");
const g = (args) => execFileSync("git", args, { cwd: root, encoding: "utf8" }).trim();
const write = (pathname, value) => { mkdirSync(resolve(root, ".figma-gate"), { recursive: true }); writeFileSync(resolve(root, pathname), JSON.stringify(value, null, 2)); };
const fetchPage = (url) => new Promise((resolvePage, rejectPage) => get(url, (response) => { let body = ""; response.setEncoding("utf8"); response.on("data", (part) => { body += part; }); response.on("end", () => response.statusCode === 200 ? resolvePage({ body, headers: response.headers }) : rejectPage(new Error("unexpected page status " + response.statusCode))); }).on("error", rejectPage));
if (!helper || !bare || !cjs || !imported || !manifestPath) process.exit(2);
const manifest = JSON.parse(readFileSync(resolve(root, manifestPath), "utf8"));
if (command === "preflight") {
  if (process.argv.length !== 8 || !implementationActor || !implementationContextId) process.exit(2);
  const vals = {}; for (const n of ${JSON.stringify(names)}) vals[n] = h("MyBrain/verify/e2e-frozen/" + n + ".json");
  write(".figma-gate/active.json", { version: 5, phase: "preflight", repository: root, runtime: { entryPath: resolve(root, "MyBrain/verify/figma-gate.mjs"), entrySha256: h("MyBrain/verify/figma-gate.mjs") }, git: { worktreeRoot: root, commit: g(["rev-parse", "HEAD"]), tree: g(["rev-parse", "HEAD^{tree}"]) }, manifestId: "p3-manifest", implementationIdentity: { actor: implementationActor, contextId: implementationContextId }, preflightId: randomUUID(), preflightAt: "2026-08-07T00:00:00.000Z", preEdit: { dirtyPaths: [], changeTargetStatus: { "src/main.html": "clean" } }, manifestSha256: vals.manifest, specSha256: h("MyBrain/verify/e2e-frozen/spec.json"), componentsSha256: vals.components, mappingSha256: vals.mapping, nodeMapSha256: h("MyBrain/verify/e2e-frozen/nodemap.json"), componentDecisionSha256: vals["component-decision"], nodeEvidenceSha256: vals["node-evidence"], layerEvidenceSha256: vals["layer-evidence"], accessibilitySha256: vals.accessibility, motionSha256: vals.motion, axeSourceSha256: vals["axe-source"], changeTargets: ["src/main.html"], benchmark: { plan: ["hero", "cards"], attempts: [] }, learningMetrics: { directViewportRuns: { hero: { pc: 1 }, cards: { pc: 1 } } } });
} else if (command === "close") {
  const state = JSON.parse(readFileSync(resolve(root, ".figma-gate/active.json"), "utf8"));
  write("MyBrain/verify/e2e-out/figma-gate-close-invocation.json", { invocationId: randomUUID() });
  const page = await fetchPage(manifest.scope.verifyUrl); const overridePath = resolve(root, "MyBrain/verify/e2e-out/page-identity-override.json"); const override = existsSync(overridePath) ? JSON.parse(readFileSync(overridePath, "utf8")) : {};
  const identity = { loadedUrl: override.loadedUrl ?? manifest.scope.verifyUrl, documentHtmlSha256: textHash(page.body), resourceUrls: override.resourceUrls ?? [manifest.scope.verifyUrl] };
  const responseHeaders = { "x-figma-p3-provider": override.providerHeader ?? page.headers["x-figma-p3-provider"], "x-figma-p3-entry-sha256": override.entrySha256Header ?? page.headers["x-figma-p3-entry-sha256"], "x-figma-p3-bundle-sha256": override.bundleMerkleRootHeader ?? page.headers["x-figma-p3-bundle-sha256"] }; const resource = { url: manifest.scope.verifyUrl, resourceType: "Document", completion: "finished", response: { url: manifest.scope.verifyUrl, status: 200, fromDiskCache: false, fromServiceWorker: false, fromPrefetchCache: false, headers: responseHeaders } }; const phase = (id, offset) => ({ id, startedAtEpochMs: 1723161600000 + offset, endedAtEpochMs: 1723161600001 + offset, webSockets: [], resources: [resource], documentResponses: [resource.response] }); identity.network = { version: 1, kind: "p3-hermetic-network-v1", expectedOrigin: new URL(manifest.scope.verifyUrl).origin, controls: { cacheDisabled: true, bypassServiceWorker: true }, providerHeaders: { providerMarker: responseHeaders["x-figma-p3-provider"], entrySha256: responseHeaders["x-figma-p3-entry-sha256"], bundleMerkleRoot: responseHeaders["x-figma-p3-bundle-sha256"] }, phases: [phase("q09-layout", 0), phase("q09-capture", 10), phase("q13-accessibility", 20), phase("q08-motion", 30)] };
  const checkpoints = {}; for (const [elementId, pid] of [["hero", 101], ["cards", 102]]) { const session = "cdp-" + pid + "-e2e"; const pathname = "MyBrain/verify/e2e-out/" + elementId + "-batch.json"; const summary = { version: 1, status: "PASS", p3Hermetic: true, url: manifest.scope.verifyUrl, checkpointElementId: elementId, preflightId: state.preflightId, browserSessionId: session, browserPid: pid, nodeVersion: process.version, browser: { source: "CDP Browser.getVersion", product: "HeadlessChrome/e2e", revision: "e2e-revision", userAgent: "e2e-agent" }, layout: { browserSessionId: session, browserPid: pid }, accessibility: { browserSessionId: session, browserPid: pid }, motion: { browserSessionId: session, browserPid: pid }, pageIdentity: identity }; write(pathname, summary); checkpoints[elementId] = { batchEvidence: { batchSummaryPath: resolve(root, pathname), batchSummarySha256: h(pathname), browserSessionId: session, browserPid: pid } }; }
  const close = { version: 1, status: "PASS", manifestId: "p3-manifest", result: { specFail: 0, layoutFail: 0, visualFail: 0 }, fileHashes: { "src/main.html": h("src/main.html") }, coverage: { targetSectionIds: ["hero"] }, checkpoints: [{ elementId: "hero", status: "PASS" }, { elementId: "cards", status: "PASS" }] }; write("MyBrain/verify/e2e-out/close.json", close);
  write(".figma-gate/active.json", { ...state, phase: "closed", fileHashes: close.fileHashes, closeReport: { path: resolve(root, "MyBrain/verify/e2e-out/close.json"), sha256: h("MyBrain/verify/e2e-out/close.json") }, checkpoints, benchmark: { plan: ["hero", "cards"], attempts: [{ elementId: "hero", outcome: "PASS", failureClass: null, at: "2026-08-07T00:01:00.000Z" }, { elementId: "cards", outcome: "PASS", failureClass: null, at: "2026-08-07T00:02:00.000Z" }] } });
} else process.exit(2);
`);
}
function decisionInputBundle(root, commit, tree, sourceSnapshot, ownerScope, baseline, execution, cleanRoomAuthorization) {
  const components = read(root, "MyBrain/verify/e2e-frozen/components.json").components.map((component) => ({ elementId: component.elementId, selector: component.selector, figmaNodeId: component.figmaNodeId, codePath: read(root, "MyBrain/verify/e2e-frozen/component-decision.json").decisions.find((decision) => decision.elementId === component.elementId).codePath })).sort((a, b) => a.elementId.localeCompare(b.elementId));
  return {
    version: 1,
    figma: { fileKey: "figma-e2e", rootNodeId: "1:2", nodeMap: evidence(root, "MyBrain/verify/e2e-frozen/nodemap.json"), metadata: evidence(root, "MyBrain/verify/e2e-frozen/metadata.json"), designContexts: [evidence(root, "MyBrain/verify/e2e-frozen/design.json")], screenshots: [evidence(root, "MyBrain/verify/e2e-frozen/screenshot.png")], assets: [] },
    sourceSnapshot: { archive: evidence(root, "MyBrain/verify/e2e-frozen/p3-source-snapshot.tar"), preImplementationProof: evidence(root, "MyBrain/verify/e2e-frozen/p3-pre-implementation.json"), git: { commit, tree } },
    scope: { specs: [evidence(root, "MyBrain/verify/e2e-frozen/spec.json")], pageCoverage: evidence(root, "MyBrain/verify/e2e-frozen/page.json"), masks: [], thresholds: evidence(root, "MyBrain/verify/e2e-frozen/thresholds.json"), frozen: { verifyUrl: providerUrl, checkpointPlan: ownerScope.checkpointPlan, changeTargets: ownerScope.changeTargets, targetSectionIds: ownerScope.targetSectionIds, components } },
    gate: { manifestId: "p3-manifest", inputs: inputs(root) },
    evaluator: { roots: evaluatorRoots(root), baselineRecord: baseline, executionBundleSha256: stableHash(execution) },
    environment: { nodeVersion: process.version, nodeExecArgv: [], chrome: { version: 1, source: "CDP Browser.getVersion", fields: ["product", "revision", "userAgent"], equality: "within-final-batches-and-across-pair" } },
    pageProvider: { kind: "hermetic-static-v1", outputRoot: "src", entryPath: "main.html", entryTargetPath: "src/main.html", targetPaths: ["src/main.html"], verifyUrl: providerUrl },
    cleanRoomAuthorization,
  };
}
function cleanRoomRoots(root) {
  const here = resolve(root);
  if (baselineWorktreeRoot && currentWorktreeRoot && (here === resolve(baselineWorktreeRoot) || here === resolve(currentWorktreeRoot))) return { baseline: canonicalP3Path(baselineWorktreeRoot), current: canonicalP3Path(currentWorktreeRoot) };
  return { baseline: canonicalP3Path(here), current: canonicalP3Path(resolve(dirname(here), `${here.split(/[\\/]/).at(-1)}-current`)) };
}
function cleanRoomConditionPlan(condition, pairId, roots) {
  const workspaceId = condition === "baseline" ? "room-a" : "room-b";
  const otherWorkspaceId = condition === "baseline" ? "room-b" : "room-a";
  return {
    condition,
    evidencePath: `MyBrain/verify/e2e-frozen/p3-clean-room-${pairId}-${condition}.json`,
    workspaceId,
    worktreeRoot: condition === "baseline" ? roots.baseline : roots.current,
    implementation: { actor: `implementation-${condition}`, contextId: `implementation-${condition}` },
    review: { actor: `review-${condition}`, contextId: `review-${condition}` },
    otherWorkspaceId,
    isolationMechanism: "Owner-controlled separate Git worktree and separate implementation/review context.",
    otherConditionArtifactsAccessible: false,
    prohibitedArtifacts: ["other-source", "other-diffs", "other-checkpoints", "other-conversation", "other-results"],
  };
}
function cleanRoomAuthorizationPlan(root, pairId) {
  const roots = cleanRoomRoots(root);
  return { version: 1, pairId, conditions: [cleanRoomConditionPlan("baseline", pairId, roots), cleanRoomConditionPlan("current", pairId, roots)] };
}
function archiveAndApprovals(root, commit, tree) {
  writeFileSync(join(root, "MyBrain/verify/e2e-frozen/p3-source-snapshot.tar"), execFileSync("git", ["archive", "--format=tar", commit], { cwd: root }));
  const scope = { manifestSha256: sha(root, "MyBrain/verify/e2e-frozen/manifest.json"), componentsSha256: sha(root, "MyBrain/verify/e2e-frozen/components.json"), pageCoverageSha256: sha(root, "MyBrain/verify/e2e-frozen/page.json"), checkpointPlan: ["hero", "cards"], changeTargets: ["src/main.html"], targetSectionIds: ["hero"] };
  const sourceSnapshot = { commit, tree, archiveSha256: sha(root, "MyBrain/verify/e2e-frozen/p3-source-snapshot.tar") };
  put(root, "MyBrain/verify/e2e-frozen/p3-pre-implementation.json", { version: 2, status: "approved", ownerApproved: true, approvedAt: "2026-08-07T00:00:00.000Z", sourceSnapshot, scope: { manifestSha256: scope.manifestSha256, componentsSha256: scope.componentsSha256, pageCoverageSha256: scope.pageCoverageSha256, checkpointPlan: scope.checkpointPlan }, unimplementedTargetPaths: ["src/main.html"], unimplementedComponents: [{ elementId: "hero", selector: ".p3-hero", figmaNodeId: "1:3", codePath: "src/main.html" }, { elementId: "cards", selector: ".p3-cards", figmaNodeId: "1:4", codePath: "src/main.html" }] });
  const baseline = writeEvaluatorBaseline(root); const execution = evaluatorExecutionBundle(root);
  const authorization = cleanRoomAuthorizationPlan(root, "p3-template");
  put(root, "MyBrain/verify/e2e-frozen/p3-owner-decision.json", { version: 2, decisionId: "J", pairId: "p3-template", status: "approved", ownerApproved: true, approvedAt: "2026-08-08T00:00:00.000Z", figma: { fileKey: "figma-e2e", rootNodeId: "1:2" }, sourceSnapshot: { ...sourceSnapshot, preImplementationProofSha256: sha(root, "MyBrain/verify/e2e-frozen/p3-pre-implementation.json") }, scope, evaluatorRootsSha256: stableHash(evaluatorRoots(root)), evaluatorBaselineSha256: baseline.sha256, evaluatorExecutionBundleSha256: stableHash(execution), cleanRoomAuthorization: authorization, cleanRoomAuthorizationStableJsonSha256: stableHash(authorization), comparisonInputBundleSha256: stableHash(decisionInputBundle(root, commit, tree, sourceSnapshot, scope, baseline, execution, authorization)) });
}
function changeApproval(root, pairId, commit, tree) {
  const pathname = `MyBrain/verify/e2e-frozen/p3-current-change-${pairId}.json`; const scope = { manifestSha256: sha(root, "MyBrain/verify/e2e-frozen/manifest.json"), componentsSha256: sha(root, "MyBrain/verify/e2e-frozen/components.json"), pageCoverageSha256: sha(root, "MyBrain/verify/e2e-frozen/page.json"), checkpointPlan: ["hero", "cards"], changeTargets: ["src/main.html"], targetSectionIds: ["hero"] };
  put(root, pathname, { version: 1, status: "approved", ownerApproved: true, approvedAt: "2026-08-07T00:00:00.000Z", pairId, evaluatedChangeId: "candidate", ownerDecisionJSha256: sha(root, `MyBrain/verify/e2e-frozen/p3-owner-decision-${pairId}.json`), sourceSnapshot: { commit, tree, archiveSha256: sha(root, "MyBrain/verify/e2e-frozen/p3-source-snapshot.tar"), preImplementationProofSha256: sha(root, "MyBrain/verify/e2e-frozen/p3-pre-implementation.json") }, scope });
  return evidence(root, pathname);
}
function pairDecision(root, pairId, commit, tree, authorization = cleanRoomAuthorizationPlan(root, pairId)) {
  const pathname = `MyBrain/verify/e2e-frozen/p3-owner-decision-${pairId}.json`;
  const scope = { manifestSha256: sha(root, "MyBrain/verify/e2e-frozen/manifest.json"), componentsSha256: sha(root, "MyBrain/verify/e2e-frozen/components.json"), pageCoverageSha256: sha(root, "MyBrain/verify/e2e-frozen/page.json"), checkpointPlan: ["hero", "cards"], changeTargets: ["src/main.html"], targetSectionIds: ["hero"] };
  const sourceSnapshot = { commit, tree, archiveSha256: sha(root, "MyBrain/verify/e2e-frozen/p3-source-snapshot.tar") }; const baseline = evidence(root, evaluatorBaselinePath); const execution = evaluatorExecutionBundle(root);
  put(root, pathname, { version: 2, decisionId: "J", pairId, status: "approved", ownerApproved: true, approvedAt: "2026-08-08T00:00:00.000Z", figma: { fileKey: "figma-e2e", rootNodeId: "1:2" }, sourceSnapshot: { ...sourceSnapshot, preImplementationProofSha256: sha(root, "MyBrain/verify/e2e-frozen/p3-pre-implementation.json") }, scope, evaluatorRootsSha256: stableHash(evaluatorRoots(root)), evaluatorBaselineSha256: baseline.sha256, evaluatorExecutionBundleSha256: stableHash(execution), cleanRoomAuthorization: authorization, cleanRoomAuthorizationStableJsonSha256: stableHash(authorization), comparisonInputBundleSha256: stableHash(decisionInputBundle(root, commit, tree, sourceSnapshot, scope, baseline, execution, authorization)) });
  return evidence(root, pathname);
}
function common(root, commit, tree, pairId, authorization) {
  return { figma: { fileKey: "figma-e2e", rootNodeId: "1:2", nodeMap: evidence(root, "MyBrain/verify/e2e-frozen/nodemap.json"), metadata: evidence(root, "MyBrain/verify/e2e-frozen/metadata.json"), designContexts: [evidence(root, "MyBrain/verify/e2e-frozen/design.json")], screenshots: [evidence(root, "MyBrain/verify/e2e-frozen/screenshot.png")], assets: [] }, sourceSnapshot: { archive: evidence(root, "MyBrain/verify/e2e-frozen/p3-source-snapshot.tar"), preImplementationProof: evidence(root, "MyBrain/verify/e2e-frozen/p3-pre-implementation.json"), git: { commit, tree } }, scope: { specs: [evidence(root, "MyBrain/verify/e2e-frozen/spec.json")], pageCoverage: evidence(root, "MyBrain/verify/e2e-frozen/page.json"), masks: [], thresholds: evidence(root, "MyBrain/verify/e2e-frozen/thresholds.json") }, gate: { manifestId: "p3-manifest", inputs: inputs(root) }, pageProvider: { kind: "hermetic-static-v1", outputRoot: "src", entryPath: "main.html" }, cleanRoomAuthorization: authorization, ownerDecisionJ: pairDecision(root, pairId, commit, tree, authorization), evaluator: { ...evaluatorRoots(root), packageLock: evidence(root, "package-lock.json"), baselineRecord: evidence(root, evaluatorBaselinePath) }, environment: { nodeVersion: process.version, nodeExecArgv: [], chrome: { version: 1, source: "CDP Browser.getVersion", fields: ["product", "revision", "userAgent"], equality: "within-final-batches-and-across-pair" } } };
}
function closeState(root, contractPath = activeContractPaths.get(root)) {
  if (!contractPath) throw new Error("P-3 E2E has no active pair-preflight contract for provider close");
  put(root, "src/main.html", "<!doctype html><html><head><title>P-3</title></head><body><main class=\"p3-hero\"><section class=\"p3-cards\">P-3</section></main></body></html>\n");
  run(root, "pair-close", contractPath, "MyBrain/verify/e2e-frozen/manifest.json");
}
function closeFails(root, expected, contractPath) {
  put(root, "src/main.html", "<!doctype html><html><head><title>P-3</title></head><body><main class=\"p3-hero\"><section class=\"p3-cards\">P-3</section></main></body></html>\n");
  fails(root, expected, "pair-close", contractPath, "MyBrain/verify/e2e-frozen/manifest.json");
}
function resetUnimplemented(root) { rmSync(join(root, "src/main.html"), { force: true }); rmSync(join(root, "MyBrain/verify/e2e-out/page-identity-override.json"), { force: true }); }
function cleanRoom(root, condition, pairId, authorization) {
  const conditionAuthorization = authorization.conditions.find((candidate) => candidate.condition === condition);
  const pathname = conditionAuthorization.evidencePath;
  const ownerDecisionPath = `MyBrain/verify/e2e-frozen/p3-owner-decision-${pairId}.json`;
  put(root, pathname, { version: 2, kind: "p3-clean-room-evidence", status: "approved", ownerApproved: true, approvedAt: "2026-08-08T00:00:01.000Z", pairId, condition, ownerDecisionJ: { path: ownerDecisionPath, fileSha256: sha(root, ownerDecisionPath) }, cleanRoomAuthorizationStableJsonSha256: stableHash(authorization), conditionAuthorization });
  return { evidence: evidence(root, pathname) };
}
function comparison(root, condition, pairId, commit, tree) {
  const authorization = cleanRoomAuthorizationPlan(root, pairId);
  const conditionAuthorization = authorization.conditions.find((candidate) => candidate.condition === condition);
  return { version: 13, pairId, condition, shared: common(root, commit, tree, pairId, authorization), run: { workspaceId: conditionAuthorization.workspaceId, implementation: conditionAuthorization.implementation, review: conditionAuthorization.review, cleanRoom: cleanRoom(root, condition, pairId, authorization), initialSourceSnapshotSha256: sha(root, "MyBrain/verify/e2e-frozen/p3-source-snapshot.tar"), evaluatedChange: condition === "baseline" ? { id: "baseline" } : { id: "candidate", approvalRecord: changeApproval(root, pairId, commit, tree) }, close: { path: "MyBrain/verify/e2e-out/close.json" }, pageProviderReceiptPath: `MyBrain/verify/e2e-out/${pairId}-${condition}-provider.json`, release: { status: "not-applicable", reason: "This isolated pilot is not a public deployment." } } };
}
function bundleScopeNegativeCase(pairId, trackedBundlePath, contents, label) {
  const root = mkdtempSync(join(tmpdir(), `figma-p3-bundle-${pairId}-`));
  try {
    fixture(root);
    // The non-target file is part of the frozen source snapshot. Once main.html
    // is implemented, finalChangeScope sees exactly the frozen target, so the
    // following failures can only come from assertHermeticBundleScope().
    put(root, trackedBundlePath, contents);
    git(root, "init"); git(root, "config", "user.email", "e2e@example.test"); git(root, "config", "user.name", "P3 E2E"); git(root, "add", "."); git(root, "commit", "-m", `tracked ${trackedBundlePath} source snapshot`);
    const commit = git(root, "rev-parse", "HEAD"); const tree = git(root, "rev-parse", "HEAD^{tree}");
    archiveAndApprovals(root, commit, tree);
    put(root, "MyBrain/verify/e2e-out/close.json", {});
    const contractPath = `MyBrain/verify/e2e-comparison/${pairId}.json`;
    put(root, contractPath, comparison(root, "baseline", pairId, commit, tree));
    resetUnimplemented(root);
    run(root, "pair-begin", contractPath);
    run(root, "pair-preflight", contractPath, "MyBrain/verify/e2e-frozen/manifest.json");
    put(root, "src/main.html", "<!doctype html><main class=\"p3-hero\"><section class=\"p3-cards\">P-3</section></main>\n");
    const readinessBefore = readinessLifecycleSnapshot(root, pairId);
    fails(root, "Comparison pre-close provider snapshot bundle file paths and frozen changeTargets", "pair-readiness", contractPath, "pre-close");
    assertReadinessDidNotMutateLifecycle(root, pairId, readinessBefore, `${label} pre-close readiness`);
    const activeBefore = readFileSync(join(root, ".figma-gate/active.json"), "utf8");
    const closePath = join(root, "MyBrain/verify/e2e-out/close.json");
    const closeBefore = existsSync(closePath) ? readFileSync(closePath).toString("base64") : null;
    const invocationPath = join(root, "MyBrain/verify/e2e-out/figma-gate-close-invocation.json");
    const closeInvocationBefore = existsSync(invocationPath) ? readFileSync(invocationPath).toString("base64") : null;
    fails(root, "P-3 provider snapshot bundle file paths and frozen changeTargets", "pair-close", contractPath, "MyBrain/verify/e2e-frozen/manifest.json");
    assertPairCloseDidNotReachGateClose(root, contractPath, activeBefore, closeBefore, closeInvocationBefore, label);
    assertAborted(root, pairId, label);
    fails(root, "cannot be reused", "pair-begin", contractPath);
  } finally {
    activeContractPaths.delete(root);
    rmSync(root, { recursive: true, force: true });
  }
}
function bundleScopeNegativeCases() {
  // This is the non-redundant legacy-source case: finalChangedSourcePaths()
  // cannot list an unchanged tracked file from the source commit.
  bundleScopeNegativeCase("p10-provider-legacy-html", "src/legacy.html", "<!doctype html><title>legacy source file</title>\n", "tracked legacy static bundle HTML");
}
const a = mkdtempSync(join(tmpdir(), "figma-p3-a-")); const b = `${a}-b`;
try {
  fixture(a); git(a, "init"); git(a, "config", "user.email", "e2e@example.test"); git(a, "config", "user.name", "P3 E2E"); git(a, "add", "."); git(a, "commit", "-m", "snapshot");
  const commit = git(a, "rev-parse", "HEAD"); const tree = git(a, "rev-parse", "HEAD^{tree}"); git(a, "worktree", "add", b, "-b", "p3-current");
  baselineWorktreeRoot = resolve(a); currentWorktreeRoot = resolve(b);
  archiveAndApprovals(a, commit, tree); for (const pathname of ["MyBrain/verify/e2e-frozen/p3-source-snapshot.tar", "MyBrain/verify/e2e-frozen/p3-pre-implementation.json", evaluatorBaselinePath, "MyBrain/verify/e2e-frozen/p3-owner-decision.json"]) copyFileSync(join(a, pathname), join(b, pathname)); put(a, "MyBrain/verify/e2e-out/close.json", {}); put(b, "MyBrain/verify/e2e-out/close.json", {});
  bundleScopeNegativeCases();
  put(a, "MyBrain/verify/e2e-comparison/p4-positive.json", comparison(a, "baseline", "p4-positive", commit, tree)); put(b, "MyBrain/verify/e2e-comparison/p4-positive.json", comparison(b, "current", "p4-positive", commit, tree));
  const baselinePositiveContract = read(a, "MyBrain/verify/e2e-comparison/p4-positive.json"); const currentPositiveContract = read(b, "MyBrain/verify/e2e-comparison/p4-positive.json"); if (baselinePositiveContract.version !== 13 || currentPositiveContract.version !== 13 || baselinePositiveContract.shared.ownerDecisionJ.sha256 !== currentPositiveContract.shared.ownerDecisionJ.sha256 || stableHash(baselinePositiveContract.shared.cleanRoomAuthorization) !== stableHash(currentPositiveContract.shared.cleanRoomAuthorization) || baselinePositiveContract.run.cleanRoom.evidence.sha256 === currentPositiveContract.run.cleanRoom.evidence.sha256) throw new Error("P-3 v13 normal A/B fixture did not bind one shared Decision J/clean-room plan with condition-specific evidence");
  const v12ContractPair = "p13-v12-contract-rejected"; const v12ContractPath = "MyBrain/verify/e2e-comparison/p13-v12-contract-rejected.json"; const v12Contract = comparison(a, "baseline", v12ContractPair, commit, tree); v12Contract.version = 12; put(a, v12ContractPath, v12Contract); fails(a, "Comparison contract version must be 13; v12 and every earlier contract are rejected without migration", "pair-begin", v12ContractPath); assertNotReserved(a, v12ContractPair, "v12 comparison contract");
  const nodeMapPath = "MyBrain/verify/e2e-frozen/nodemap.json"; const originalNodeMap = read(a, nodeMapPath); const canonicalRootMismatchPair = "p13-node-map-canonical-root-rejected"; const canonicalRootMismatchPath = "MyBrain/verify/e2e-comparison/p13-node-map-canonical-root-rejected.json"; put(a, nodeMapPath, { ...originalNodeMap, figma: { ...originalNodeMap.figma, canonicalRootNodeId: "9:9" } }); put(a, canonicalRootMismatchPath, comparison(a, "baseline", canonicalRootMismatchPair, commit, tree)); fails(a, "Comparison Figma fileKey/canonicalRootNodeId must match frozen node map v2", "p3-decision-input-plan", canonicalRootMismatchPath); assertNotReserved(a, canonicalRootMismatchPair, "canonicalRootNodeId mismatch"); put(a, nodeMapPath, originalNodeMap);
  const jsonHashPlan = JSON.parse(run(a, "p3-json-hash", "MyBrain/verify/e2e-frozen/metadata.json")); if (jsonHashPlan.path !== "MyBrain/verify/e2e-frozen/metadata.json" || !/^[a-f0-9]{64}$/.test(jsonHashPlan.fileSha256) || !/^[a-f0-9]{64}$/.test(jsonHashPlan.stableJsonSha256)) throw new Error("P-3 JSON hash plan did not return byte and stable JSON SHA-256 values");
  const evaluatorPlan = JSON.parse(run(a, "p3-evaluator-plan", "MyBrain/verify/e2e-comparison/p4-positive.json")); if (evaluatorPlan.baselineRecordTemplate?.status !== "draft" || evaluatorPlan.baselineRecordTemplate?.ownerApproved !== false || evaluatorPlan.baselineRecordTemplate?.artifacts?.length !== evaluators.length || !/^[a-f0-9]{64}$/.test(evaluatorPlan.executionBundleSha256)) throw new Error("P-3 evaluator plan did not return the 12-root draft baseline template");
  const decisionPlan = JSON.parse(run(a, "p3-decision-input-plan", "MyBrain/verify/e2e-comparison/p4-positive.json")); if (decisionPlan.pairId !== "p4-positive" || decisionPlan.ownerDecisionJTemplate?.version !== 2 || decisionPlan.ownerDecisionJTemplate?.pairId !== "p4-positive" || decisionPlan.ownerDecisionJTemplate?.status !== "draft" || decisionPlan.ownerDecisionJTemplate?.ownerApproved !== false || decisionPlan.ownerDecisionJTemplate?.cleanRoomAuthorization?.conditions?.map((entry) => entry.condition).join(",") !== "baseline,current" || !/^[a-f0-9]{64}$/.test(decisionPlan.cleanRoomAuthorizationStableJsonSha256) || !/^[a-f0-9]{64}$/.test(decisionPlan.comparisonInputBundleSha256)) throw new Error("P-3 decision input plan did not return the pair-specific draft Decision J v2 template and clean-room authorization");
  const decisionCandidatePair = "p13-decision-candidate"; const decisionCandidateRuntimePath = "MyBrain/verify/e2e-comparison/p13-decision-candidate-runtime.json"; const decisionCandidateRuntime = comparison(a, "baseline", decisionCandidatePair, commit, tree); put(a, decisionCandidateRuntimePath, decisionCandidateRuntime); const decisionCandidateEnvelopePath = "MyBrain/verify/p3-drafts/p13-decision-candidate.json"; const decisionCandidatePreImplementationPath = "MyBrain/verify/p3-drafts/p13-pre-implementation.json"; const decisionCandidateEvaluatorPath = "MyBrain/verify/p3-drafts/p13-evaluator-baseline.json"; const decisionCandidatePreImplementation = read(a, "MyBrain/verify/e2e-frozen/p3-pre-implementation.json"); decisionCandidatePreImplementation.status = "draft"; decisionCandidatePreImplementation.ownerApproved = false; delete decisionCandidatePreImplementation.approvedAt; put(a, decisionCandidatePreImplementationPath, decisionCandidatePreImplementation); const decisionCandidateEvaluator = read(a, evaluatorBaselinePath); decisionCandidateEvaluator.status = "draft"; decisionCandidateEvaluator.ownerApproved = false; delete decisionCandidateEvaluator.approvedAt; put(a, decisionCandidateEvaluatorPath, decisionCandidateEvaluator); const decisionCandidateShared = JSON.parse(JSON.stringify(decisionCandidateRuntime.shared)); decisionCandidateShared.sourceSnapshot.preImplementationProof = evidence(a, decisionCandidatePreImplementationPath); decisionCandidateShared.evaluator.baselineRecord = evidence(a, decisionCandidateEvaluatorPath); put(a, decisionCandidateEnvelopePath, { schema: "p3-comparison-contract-draft/v13", draftOnly: true, status: "draft", ownerApproved: false, pairIdCandidate: decisionCandidatePair, condition: "baseline", candidateComparison: { version: 13, pairId: decisionCandidatePair, condition: "baseline", shared: decisionCandidateShared } }); fails(a, "Comparison contract version must be 13", "p3-decision-input-plan", decisionCandidateEnvelopePath); const decisionCandidate = JSON.parse(run(a, "p3-decision-candidate-plan", decisionCandidateEnvelopePath, decisionCandidatePreImplementationPath, decisionCandidateEvaluatorPath)); if (decisionCandidate.kind !== "p3-decision-j-candidate-plan" || decisionCandidate.readOnly !== true || decisionCandidate.candidateOnly !== true || decisionCandidate.runtimeEligible !== false || !/^[a-f0-9]{64}$/.test(decisionCandidate.comparisonInputBundleCandidateSha256) || Object.prototype.hasOwnProperty.call(decisionCandidate.ownerDecisionJCandidateTemplate, "comparisonInputBundleSha256") || decisionCandidate.ownerDecisionJCandidateTemplate?.kind !== "p3-owner-decision-j-candidate" || decisionCandidate.ownerDecisionJCandidateTemplate?.draftOnly !== true || decisionCandidate.ownerDecisionJCandidateTemplate?.candidateOnly !== true || decisionCandidate.ownerDecisionJCandidateTemplate?.runtimeEligible !== false || decisionCandidate.ownerDecisionJCandidateTemplate?.status !== "candidate" || !/^[a-f0-9]{64}$/.test(decisionCandidate.ownerDecisionJCandidateTemplate?.comparisonInputBundleCandidateSha256)) throw new Error("P-3 decision candidate plan did not emit a clearly non-runtime candidate template"); assertNotReserved(a, decisionCandidatePair, "P-3 decision candidate plan");
  const legacyDecisionCandidateEnvelopePath = "MyBrain/verify/p3-drafts/p13-decision-candidate-v12-schema.json"; const legacyDecisionCandidateEnvelope = read(a, decisionCandidateEnvelopePath); legacyDecisionCandidateEnvelope.schema = "p3-comparison-contract-draft/v12"; put(a, legacyDecisionCandidateEnvelopePath, legacyDecisionCandidateEnvelope); fails(a, "P-3 decision candidate comparison contract schema must be p3-comparison-contract-draft/v13; v12 and every earlier candidate schema are rejected without migration", "p3-decision-candidate-plan", legacyDecisionCandidateEnvelopePath, decisionCandidatePreImplementationPath, decisionCandidateEvaluatorPath); assertNotReserved(a, decisionCandidatePair, "v12 decision candidate envelope");
  const readiness = JSON.parse(run(a, "pair-readiness", "MyBrain/verify/e2e-comparison/p4-positive.json", "pre-begin"));
  if (readiness.kind !== "p3-pair-readiness" || readiness.readOnly !== true || readiness.sideEffects?.ledger?.created !== false || readiness.sideEffects?.ledger?.modified !== false || readiness.sideEffects?.pairLock?.created !== false || readiness.sideEffects?.pairLock?.modified !== false || readiness.sideEffects?.gateState?.created !== false || readiness.sideEffects?.gateState?.modified !== false) throw new Error("P-3 pair readiness did not declare its read-only lifecycle boundary");
  if (readiness.preImplementation?.sourceWorktree?.status !== "verified-clean-before-implementation" || readiness.preImplementation.sourceWorktree.changedSourcePaths?.length !== 0) throw new Error("P-3 pair readiness did not verify the clean pre-implementation source worktree");
  if (readiness.pageProvider?.structuralAlignment?.entryTargetPath !== "src/main.html" || readiness.pageProvider?.structuralAlignment?.targetPaths?.join("\n") !== "src/main.html") throw new Error("P-3 pair readiness did not verify the provider entry/target structural alignment");
  if (readiness.pageProvider?.snapshotBundle?.status !== "deferred-until-pair-close" || readiness.finalChangeScope?.status !== "deferred-until-pair-close") throw new Error("P-3 pair readiness claimed a post-implementation bundle or final-change result before implementation");
  assertReadinessDidNotCreateLifecycleArtifacts(a, "p4-positive", "P-3 pair readiness");
  const originalReadinessGitignore = readFileSync(join(a, ".gitignore"), "utf8"); const ignoredReadinessPath = "MyBrain/verify/e2e-comparison/p10-readiness-ignored.json";
  writeFileSync(join(a, ".gitignore"), `${originalReadinessGitignore}p10-ignored-runtime.css\n`, "utf8"); put(a, "p10-ignored-runtime.css", ".p3-hero { color: #000; }\n"); put(a, ignoredReadinessPath, comparison(a, "baseline", "p10-readiness-ignored", commit, tree));
  fails(a, "ignored runtime artifact", "pair-readiness", ignoredReadinessPath, "pre-begin");
  assertReadinessDidNotCreateLifecycleArtifacts(a, "p10-readiness-ignored", "ignored P-3 pair readiness");
  fails(a, "ignored runtime artifact", "pair-begin", ignoredReadinessPath);
  assertReadinessDidNotCreateLifecycleArtifacts(a, "p10-readiness-ignored", "ignored P-3 pair-begin before reservation");
  rmSync(join(a, "p10-ignored-runtime.css"), { force: true }); writeFileSync(join(a, ".gitignore"), originalReadinessGitignore, "utf8");
  const jHashMismatchPath = "MyBrain/verify/e2e-comparison/p10-j-clean-room-hash-mismatch.json"; const jHashMismatch = comparison(a, "baseline", "p10-j-clean-room-hash-mismatch", commit, tree); const jHashMismatchRecord = read(a, jHashMismatch.shared.ownerDecisionJ.path); jHashMismatchRecord.cleanRoomAuthorizationStableJsonSha256 = "0".repeat(64); put(a, jHashMismatch.shared.ownerDecisionJ.path, jHashMismatchRecord); jHashMismatch.shared.ownerDecisionJ.sha256 = sha(a, jHashMismatch.shared.ownerDecisionJ.path); put(a, jHashMismatchPath, jHashMismatch); fails(a, "Owner decision J record clean-room authorization hash differs", "pair-begin", jHashMismatchPath); assertAborted(a, "p10-j-clean-room-hash-mismatch", "Decision J clean-room authorization hash mismatch");
  const sharedPlanMismatchPath = "MyBrain/verify/e2e-comparison/p10-shared-clean-room-plan-mismatch.json"; const sharedPlanMismatch = comparison(a, "baseline", "p10-shared-clean-room-plan-mismatch", commit, tree); sharedPlanMismatch.shared.cleanRoomAuthorization.conditions[0].isolationMechanism = "Owner-controlled separate Git worktree and context; tampered but internally valid plan."; put(a, sharedPlanMismatchPath, sharedPlanMismatch); fails(a, "Owner decision J record clean-room authorization differs", "pair-begin", sharedPlanMismatchPath); assertAborted(a, "p10-shared-clean-room-plan-mismatch", "shared clean-room authorization mismatch");
  const baselineEvidencePath = "MyBrain/verify/e2e-comparison/p10-baseline-clean-room-owner-approval.json"; const baselineEvidence = comparison(a, "baseline", "p10-baseline-clean-room-owner-approval", commit, tree); const invalidBaselineEvidence = read(a, baselineEvidence.run.cleanRoom.evidence.path); invalidBaselineEvidence.ownerApproved = false; put(a, baselineEvidence.run.cleanRoom.evidence.path, invalidBaselineEvidence); baselineEvidence.run.cleanRoom.evidence.sha256 = sha(a, baselineEvidence.run.cleanRoom.evidence.path); put(a, baselineEvidencePath, baselineEvidence); resetUnimplemented(a); fails(a, "Comparison clean-room evidence must be owner-approved version 2", "pair-begin", baselineEvidencePath); assertAborted(a, "p10-baseline-clean-room-owner-approval", "unapproved baseline clean-room evidence");
  const currentEvidencePath = "MyBrain/verify/e2e-comparison/p10-current-clean-room-owner-approval.json"; put(a, currentEvidencePath, comparison(a, "baseline", "p10-current-clean-room-owner-approval", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", currentEvidencePath); const invalidCurrentEvidence = comparison(b, "current", "p10-current-clean-room-owner-approval", commit, tree); const invalidCurrentCleanRoomEvidence = read(b, invalidCurrentEvidence.run.cleanRoom.evidence.path); invalidCurrentCleanRoomEvidence.ownerApproved = false; put(b, invalidCurrentEvidence.run.cleanRoom.evidence.path, invalidCurrentCleanRoomEvidence); invalidCurrentEvidence.run.cleanRoom.evidence.sha256 = sha(b, invalidCurrentEvidence.run.cleanRoom.evidence.path); put(b, currentEvidencePath, invalidCurrentEvidence); resetUnimplemented(b); rmSync(join(b, ".figma-gate"), { recursive: true, force: true }); fails(b, "Comparison clean-room evidence must be owner-approved version 2", "pair-preflight", currentEvidencePath, "MyBrain/verify/e2e-frozen/manifest.json"); assertAborted(a, "p10-current-clean-room-owner-approval", "unapproved current clean-room evidence before figma-gate preflight"); if (existsSync(join(b, ".figma-gate", "active.json"))) throw new Error("unapproved current clean-room evidence reached figma-gate preflight");
  const draftContractPair = "p10-draft-contract"; const draftContractPath = "MyBrain/verify/p3-drafts/p10-draft-contract.json"; put(a, draftContractPath, comparison(a, "baseline", draftContractPair, commit, tree)); fails(a, "comparison contract path must not reference a draft artifact", "pair-begin", draftContractPath); assertNotReserved(a, draftContractPair, "draft comparison contract"); rmSync(join(a, "MyBrain/verify/p3-drafts"), { recursive: true, force: true });
  const draftDecisionPair = "p10-draft-owner-decision"; const draftDecisionPath = "MyBrain/verify/e2e-comparison/p10-draft-owner-decision.json"; const draftDecisionContract = comparison(a, "baseline", draftDecisionPair, commit, tree); const draftDecisionRecordPath = "MyBrain/verify/p3-drafts/p10-owner-decision.json"; put(a, draftDecisionRecordPath, read(a, draftDecisionContract.shared.ownerDecisionJ.path)); draftDecisionContract.shared.ownerDecisionJ = evidence(a, draftDecisionRecordPath); put(a, draftDecisionPath, draftDecisionContract); fails(a, "contains a forbidden draft path", "pair-begin", draftDecisionPath); assertNotReserved(a, draftDecisionPair, "draft owner decision J"); rmSync(join(a, "MyBrain/verify/p3-drafts"), { recursive: true, force: true });
  const draftCleanRoomPair = "p10-draft-clean-room-evidence"; const draftCleanRoomPath = "MyBrain/verify/e2e-comparison/p10-draft-clean-room-evidence.json"; const draftCleanRoomContract = comparison(a, "baseline", draftCleanRoomPair, commit, tree); const draftCleanRoomEvidencePath = "MyBrain/verify/p3-drafts/p10-clean-room-evidence.json"; put(a, draftCleanRoomEvidencePath, read(a, draftCleanRoomContract.run.cleanRoom.evidence.path)); draftCleanRoomContract.run.cleanRoom.evidence = evidence(a, draftCleanRoomEvidencePath); put(a, draftCleanRoomPath, draftCleanRoomContract); fails(a, "contains a forbidden draft path", "pair-begin", draftCleanRoomPath); assertNotReserved(a, draftCleanRoomPair, "draft clean-room evidence"); rmSync(join(a, "MyBrain/verify/p3-drafts"), { recursive: true, force: true });
  const draftPlanPair = "p10-draft-clean-room-authorization-path"; const draftPlanPath = "MyBrain/verify/e2e-comparison/p10-draft-clean-room-authorization-path.json"; const draftPlanContract = comparison(a, "baseline", draftPlanPair, commit, tree); draftPlanContract.shared.cleanRoomAuthorization.conditions[0].evidencePath = "MyBrain/verify/p3-drafts/p10-clean-room-plan-evidence.json"; put(a, draftPlanPath, draftPlanContract); fails(a, "contains a forbidden draft path", "pair-begin", draftPlanPath); assertNotReserved(a, draftPlanPair, "draft clean-room authorization evidence path");
  const draftMarkerPair = "p10-draft-marker-before-reservation"; const draftMarkerPath = "MyBrain/verify/e2e-comparison/p10-draft-marker-before-reservation.json"; const draftMarkerContract = comparison(a, "baseline", draftMarkerPair, commit, tree); const draftMarkerDecision = read(a, draftMarkerContract.shared.ownerDecisionJ.path); draftMarkerDecision.draftOnly = false; put(a, draftMarkerContract.shared.ownerDecisionJ.path, draftMarkerDecision); draftMarkerContract.shared.ownerDecisionJ.sha256 = sha(a, draftMarkerContract.shared.ownerDecisionJ.path); put(a, draftMarkerPath, draftMarkerContract); fails(a, "forbidden draft marker", "pair-begin", draftMarkerPath); assertNotReserved(a, draftMarkerPair, "draftOnly:false owner decision J");
  const draftArrayPair = "p10-draft-array-marker-before-reservation"; const draftArrayPath = "MyBrain/verify/e2e-comparison/p10-draft-array-marker-before-reservation.json"; const draftArrayContract = comparison(a, "baseline", draftArrayPair, commit, tree); draftArrayContract.shared.scope.masks = ["OWNER_INPUT_REQUIRED__MASK_REVIEW"]; put(a, draftArrayPath, draftArrayContract); fails(a, "forbidden owner-input placeholder", "pair-begin", draftArrayPath); assertNotReserved(a, draftArrayPair, "owner-input placeholder in comparison contract array");
  const postBeginDraftPair = "p10-draft-marker-after-reservation"; const postBeginDraftPath = "MyBrain/verify/e2e-comparison/p10-draft-marker-after-reservation.json"; put(a, postBeginDraftPath, comparison(a, "baseline", postBeginDraftPair, commit, tree)); resetUnimplemented(a); run(a, "pair-begin", postBeginDraftPath); const postBeginDraftContract = read(a, postBeginDraftPath); const postBeginDraftDecision = read(a, postBeginDraftContract.shared.ownerDecisionJ.path); postBeginDraftDecision._draftOnly = false; put(a, postBeginDraftContract.shared.ownerDecisionJ.path, postBeginDraftDecision); postBeginDraftContract.shared.ownerDecisionJ.sha256 = sha(a, postBeginDraftContract.shared.ownerDecisionJ.path); put(a, postBeginDraftPath, postBeginDraftContract); fails(a, "forbidden draft marker", "pair-preflight", postBeginDraftPath, "MyBrain/verify/e2e-frozen/manifest.json"); assertAborted(a, postBeginDraftPair, "draft marker after pair reservation");
  const caseVariantProviderPath = "MyBrain/verify/e2e-comparison/p9-case-variant-provider.json"; const caseVariantProvider = comparison(a, "baseline", "p9-case-variant-provider", commit, tree); caseVariantProvider.shared.pageProvider.outputRoot = "mybrain/verify/e2e-out"; put(a, caseVariantProviderPath, caseVariantProvider); fails(a, "outputRoot must be a real source-side descendant", "pair-begin", caseVariantProviderPath);
  const decoyOutputPath = "MyBrain/verify/e2e-comparison/p9-decoy-output.json"; const decoyOutput = comparison(a, "baseline", "p9-decoy-output", commit, tree); decoyOutput.shared.pageProvider.outputRoot = "static-decoy"; decoyOutput.shared.pageProvider.entryPath = "index.html"; put(a, decoyOutputPath, decoyOutput); fails(a, "outputRoot must contain every frozen changeTarget", "pair-begin", decoyOutputPath);
  const legacyLockPair = "p10-legacy-v4-pair-lock"; const legacyLockContractPath = "MyBrain/verify/e2e-comparison/p10-legacy-v4-pair-lock.json"; put(a, legacyLockContractPath, comparison(a, "baseline", legacyLockPair, commit, tree)); const legacyLockPath = pairLockPath(a, legacyLockPair); mkdirSync(dirname(legacyLockPath), { recursive: true }); writeFileSync(legacyLockPath, `${JSON.stringify({ version: 4, pairId: legacyLockPair, contractPath: legacyLockContractPath, ledgerPath: resolve(a, ".git", "figma-p3-comparison-ledger.jsonl"), reservedAt: "2026-08-08T00:00:00.000Z" }, null, 2)}\n`, "utf8"); fails(a, "already reserved and cannot be reused", "pair-begin", legacyLockContractPath); fails(a, "pre-v13 fixed reservation and cannot continue", "pair-preflight", legacyLockContractPath, "MyBrain/verify/e2e-frozen/manifest.json"); if (read(a, ".git/figma-p3-comparison-pair-locks/" + createHash("sha256").update(legacyLockPair).digest("hex") + ".json").version !== 4 || existsSync(join(a, ".git", "figma-p3-comparison-ledger.jsonl")) && ledgerRecords(a).some((record) => record.pairId === legacyLockPair)) throw new Error("legacy v4 pair lock was replaced or continued under v13"); rmSync(legacyLockPath, { force: true });
  const legacyStatePair = "p11-legacy-active-state"; const legacyStateContractPath = "MyBrain/verify/e2e-comparison/p11-legacy-active-state.json"; put(a, legacyStateContractPath, comparison(a, "baseline", legacyStatePair, commit, tree)); resetUnimplemented(a); run(a, "pair-begin", legacyStateContractPath); run(a, "pair-preflight", legacyStateContractPath, "MyBrain/verify/e2e-frozen/manifest.json"); closeState(a); const legacyState = read(a, ".figma-gate/active.json"); legacyState.version = 4; put(a, ".figma-gate/active.json", legacyState); fails(a, "Comparison report requires a v13 figma-gate active state version 5; v4 and every earlier active state are rejected without migration", "report", "MyBrain/verify/e2e-out/p11-legacy-active-state.json", legacyStateContractPath); assertAborted(a, legacyStatePair, "legacy v4 active state");
  resetUnimplemented(a); run(a, "pair-begin", "MyBrain/verify/e2e-comparison/p4-positive.json"); run(a, "pair-preflight", "MyBrain/verify/e2e-comparison/p4-positive.json", "MyBrain/verify/e2e-frozen/manifest.json"); run(b, "pair-preflight", "MyBrain/verify/e2e-comparison/p4-positive.json", "MyBrain/verify/e2e-frozen/manifest.json");
  const freshPairLock = read(a, ".git/figma-p3-comparison-pair-locks/" + createHash("sha256").update("p4-positive").digest("hex") + ".json"); if (freshPairLock.version !== 5 || freshPairLock.contractVersion !== 13 || freshPairLock.pairId !== "p4-positive") throw new Error("fresh P-3 pair-begin did not write a v5 comparison-contract-v13 fixed pair lock");
  const implementationHtml = "<!doctype html><html><head><title>P-3</title></head><body><main class=\"p3-hero\"><section class=\"p3-cards\">P-3</section></main></body></html>\n";
  put(a, "src/main.html", implementationHtml); const baselineReadinessBefore = readinessLifecycleSnapshot(a, "p4-positive"); const baselinePreCloseReadiness = JSON.parse(run(a, "pair-readiness", "MyBrain/verify/e2e-comparison/p4-positive.json", "pre-close"));
  if (baselinePreCloseReadiness.finalChangeScope?.status !== "verified-before-pair-close" || baselinePreCloseReadiness.pageProvider?.snapshotBundle?.status !== "verified-before-pair-close" || baselinePreCloseReadiness.pageProvider.snapshotBundle.entries?.map((entry) => entry.path).join("\n") !== "main.html" || !/^[a-f0-9]{64}$/.test(baselinePreCloseReadiness.pageProvider.snapshotBundle.entrySha256)) throw new Error("P-3 pre-close pair readiness did not verify final scope and provider bundle");
  assertReadinessDidNotMutateLifecycle(a, "p4-positive", baselineReadinessBefore, "baseline P-3 pre-close pair readiness");
  put(b, "src/main.html", implementationHtml); const currentReadinessBefore = readinessLifecycleSnapshot(b, "p4-positive"); const currentPreCloseReadiness = JSON.parse(run(b, "pair-readiness", "MyBrain/verify/e2e-comparison/p4-positive.json", "pre-close"));
  if (currentPreCloseReadiness.finalChangeScope?.status !== "verified-before-pair-close" || currentPreCloseReadiness.pageProvider?.snapshotBundle?.status !== "verified-before-pair-close") throw new Error("current P-3 pre-close pair readiness did not verify final scope and provider bundle");
  assertReadinessDidNotMutateLifecycle(b, "p4-positive", currentReadinessBefore, "current P-3 pre-close pair readiness");
  run(a, "pair-close", "MyBrain/verify/e2e-comparison/p4-positive.json", "MyBrain/verify/e2e-frozen/manifest.json"); run(b, "pair-close", "MyBrain/verify/e2e-comparison/p4-positive.json", "MyBrain/verify/e2e-frozen/manifest.json"); put(a, "MyBrain/verify/e2e-comparison/p4-positive.json", comparison(a, "baseline", "p4-positive", commit, tree)); put(b, "MyBrain/verify/e2e-comparison/p4-positive.json", comparison(b, "current", "p4-positive", commit, tree));
  run(a, "report", "MyBrain/verify/e2e-out/baseline.json", "MyBrain/verify/e2e-comparison/p4-positive.json"); run(b, "report", "MyBrain/verify/e2e-out/current.json", "MyBrain/verify/e2e-comparison/p4-positive.json"); const output = run(a, "compare", join(a, "MyBrain/verify/e2e-out/baseline.json"), join(b, "MyBrain/verify/e2e-out/current.json"), "MyBrain/verify/e2e-comparison/p4-positive.json"); if (!output.includes("p4-positive")) throw new Error("positive comparison missing");
  const completeComparablePair = (pairId) => {
    const contractPath = `MyBrain/verify/e2e-comparison/${pairId}.json`; const baselineReportPath = `MyBrain/verify/e2e-out/${pairId}-baseline.json`; const currentReportPath = `MyBrain/verify/e2e-out/${pairId}-current.json`;
    put(a, contractPath, comparison(a, "baseline", pairId, commit, tree)); put(b, contractPath, comparison(b, "current", pairId, commit, tree)); resetUnimplemented(a); resetUnimplemented(b); run(a, "pair-begin", contractPath); run(a, "pair-preflight", contractPath, "MyBrain/verify/e2e-frozen/manifest.json"); run(b, "pair-preflight", contractPath, "MyBrain/verify/e2e-frozen/manifest.json"); put(a, "src/main.html", implementationHtml); put(b, "src/main.html", implementationHtml); run(a, "pair-close", contractPath, "MyBrain/verify/e2e-frozen/manifest.json"); run(b, "pair-close", contractPath, "MyBrain/verify/e2e-frozen/manifest.json"); run(a, "report", baselineReportPath, contractPath); run(b, "report", currentReportPath, contractPath);
    return { contractPath, baselineReportPath, currentReportPath };
  };
  const compareDecisionMismatch = completeComparablePair("p10-compare-decision-binding"); const decisionMismatchReport = read(b, compareDecisionMismatch.currentReportPath); decisionMismatchReport.comparison.ownerDecisionJFileSha256 = "0".repeat(64); put(b, compareDecisionMismatch.currentReportPath, decisionMismatchReport); fails(a, "frozen shared evidence, owner decision, clean-room authorization, or checkpoint plan differs", "compare", join(a, compareDecisionMismatch.baselineReportPath), join(b, compareDecisionMismatch.currentReportPath), compareDecisionMismatch.contractPath); assertAborted(a, "p10-compare-decision-binding", "compare owner decision binding mismatch");
  const comparePlanMismatch = completeComparablePair("p10-compare-clean-room-plan-binding"); const planMismatchReport = read(b, comparePlanMismatch.currentReportPath); planMismatchReport.comparison.cleanRoomAuthorizationStableJsonSha256 = "0".repeat(64); put(b, comparePlanMismatch.currentReportPath, planMismatchReport); fails(a, "frozen shared evidence, owner decision, clean-room authorization, or checkpoint plan differs", "compare", join(a, comparePlanMismatch.baselineReportPath), join(b, comparePlanMismatch.currentReportPath), comparePlanMismatch.contractPath); assertAborted(a, "p10-compare-clean-room-plan-binding", "compare clean-room authorization binding mismatch");
  const compareEvidenceMismatch = completeComparablePair("p10-compare-clean-room-evidence-binding"); const evidenceMismatchReport = read(b, compareEvidenceMismatch.currentReportPath); evidenceMismatchReport.comparison.cleanRoomEvidenceSha256 = "0".repeat(64); evidenceMismatchReport.comparison.run.cleanRoom.evidence.sha256 = "0".repeat(64); put(b, compareEvidenceMismatch.currentReportPath, evidenceMismatchReport); fails(a, "pair ledger is not an active matching A/B pair", "compare", join(a, compareEvidenceMismatch.baselineReportPath), join(b, compareEvidenceMismatch.currentReportPath), compareEvidenceMismatch.contractPath); assertAborted(a, "p10-compare-clean-room-evidence-binding", "compare clean-room evidence ledger binding mismatch");
  fails(a, "cannot be reused", "pair-begin", "MyBrain/verify/e2e-comparison/p4-positive.json");
  const ignoredPreflightPath = "MyBrain/verify/e2e-comparison/p10-ignored-preflight.json"; resetUnimplemented(a); put(a, ignoredPreflightPath, comparison(a, "baseline", "p10-ignored-preflight", commit, tree)); run(a, "pair-begin", ignoredPreflightPath); rmSync(join(a, ".figma-gate"), { recursive: true, force: true });
  const originalPreflightGitignore = readFileSync(join(a, ".gitignore"), "utf8"); writeFileSync(join(a, ".gitignore"), `${originalPreflightGitignore}p10-ignored-preflight.css\n`, "utf8"); put(a, "p10-ignored-preflight.css", ".p3-hero { color: #000; }\n");
  fails(a, "ignored runtime artifact", "pair-preflight", ignoredPreflightPath, "MyBrain/verify/e2e-frozen/manifest.json"); assertAborted(a, "p10-ignored-preflight", "ignored artifact before figma-gate preflight"); if (existsSync(join(a, ".figma-gate", "active.json"))) throw new Error("ignored artifact reached figma-gate preflight");
  rmSync(join(a, "p10-ignored-preflight.css"), { force: true }); writeFileSync(join(a, ".gitignore"), originalPreflightGitignore, "utf8");
  const outsideOutputPath = "MyBrain/verify/e2e-comparison/p9-outside-output-file.json"; resetUnimplemented(a); put(a, outsideOutputPath, comparison(a, "baseline", "p9-outside-output-file", commit, tree)); run(a, "pair-begin", outsideOutputPath); run(a, "pair-preflight", outsideOutputPath, "MyBrain/verify/e2e-frozen/manifest.json"); put(a, "src/main.html", "<!doctype html><main class=\"p3-hero\"><section class=\"p3-cards\">P-3</section></main>\n"); put(a, "styles/decoy.css", ".decoy { color: red; }\n"); const extraReadinessBefore = readinessLifecycleSnapshot(a, "p9-outside-output-file"); fails(a, "final Git change scope and frozen change targets", "pair-readiness", outsideOutputPath, "pre-close"); assertReadinessDidNotMutateLifecycle(a, "p9-outside-output-file", extraReadinessBefore, "out-of-scope P-3 pre-close pair readiness"); const extraGateBefore = readFileSync(join(a, ".figma-gate/active.json"), "utf8"); const extraCloseBefore = existsSync(join(a, "MyBrain/verify/e2e-out/close.json")) ? readFileSync(join(a, "MyBrain/verify/e2e-out/close.json")).toString("base64") : null; const extraCloseInvocationBefore = existsSync(join(a, "MyBrain/verify/e2e-out/figma-gate-close-invocation.json")) ? readFileSync(join(a, "MyBrain/verify/e2e-out/figma-gate-close-invocation.json")).toString("base64") : null; fails(a, "final Git change scope and frozen change targets", "pair-close", outsideOutputPath, "MyBrain/verify/e2e-frozen/manifest.json"); assertPairCloseDidNotReachGateClose(a, outsideOutputPath, extraGateBefore, extraCloseBefore, extraCloseInvocationBefore, "out-of-output-root P-3 change"); assertAborted(a, "p9-outside-output-file", "out-of-output-root P-3 change"); fails(a, "cannot be reused", "pair-begin", outsideOutputPath); rmSync(join(a, "styles/decoy.css"), { force: true }); resetUnimplemented(a);
  const extraBundlePath = "MyBrain/verify/e2e-comparison/p10-extra-bundle-css.json"; resetUnimplemented(a); put(a, extraBundlePath, comparison(a, "baseline", "p10-extra-bundle-css", commit, tree)); run(a, "pair-begin", extraBundlePath); run(a, "pair-preflight", extraBundlePath, "MyBrain/verify/e2e-frozen/manifest.json"); put(a, "src/main.html", "<!doctype html><main class=\"p3-hero\"><section class=\"p3-cards\">P-3</section></main>\n"); put(a, "src/extra.css", ".extra { display: block; }\n"); const extraBundleReadinessBefore = readinessLifecycleSnapshot(a, "p10-extra-bundle-css"); fails(a, "final Git change scope and frozen change targets", "pair-readiness", extraBundlePath, "pre-close"); assertReadinessDidNotMutateLifecycle(a, "p10-extra-bundle-css", extraBundleReadinessBefore, "extra static bundle CSS pre-close readiness"); const extraBundleGateBefore = readFileSync(join(a, ".figma-gate/active.json"), "utf8"); const extraBundleCloseBefore = existsSync(join(a, "MyBrain/verify/e2e-out/close.json")) ? readFileSync(join(a, "MyBrain/verify/e2e-out/close.json")).toString("base64") : null; const extraBundleCloseInvocationBefore = existsSync(join(a, "MyBrain/verify/e2e-out/figma-gate-close-invocation.json")) ? readFileSync(join(a, "MyBrain/verify/e2e-out/figma-gate-close-invocation.json")).toString("base64") : null; fails(a, "final Git change scope and frozen change targets", "pair-close", extraBundlePath, "MyBrain/verify/e2e-frozen/manifest.json"); assertPairCloseDidNotReachGateClose(a, extraBundlePath, extraBundleGateBefore, extraBundleCloseBefore, extraBundleCloseInvocationBefore, "extra static bundle CSS"); assertAborted(a, "p10-extra-bundle-css", "extra static bundle CSS"); fails(a, "cannot be reused", "pair-begin", extraBundlePath); rmSync(join(a, "src/extra.css"), { force: true }); resetUnimplemented(a);
  const missingProviderPath = "MyBrain/verify/e2e-comparison/p9-missing-provider-marker.json"; resetUnimplemented(a); put(a, missingProviderPath, comparison(a, "baseline", "p9-missing-provider-marker", commit, tree)); run(a, "pair-begin", missingProviderPath); run(a, "pair-preflight", missingProviderPath, "MyBrain/verify/e2e-frozen/manifest.json"); put(a, "MyBrain/verify/e2e-out/page-identity-override.json", { providerHeader: "0".repeat(64) }); closeFails(a, "P-3 response headers differ from the frozen provider bundle", missingProviderPath); assertAborted(a, "p9-missing-provider-marker", "mismatched provider response header");
  const externalProviderPath = "MyBrain/verify/e2e-comparison/p9-external-provider-resource.json"; resetUnimplemented(a); put(a, externalProviderPath, comparison(a, "baseline", "p9-external-provider-resource", commit, tree)); run(a, "pair-begin", externalProviderPath); run(a, "pair-preflight", externalProviderPath, "MyBrain/verify/e2e-frozen/manifest.json"); put(a, "MyBrain/verify/e2e-out/page-identity-override.json", { resourceUrls: ["https://example.test/not-hermetic.css"] }); closeFails(a, "measured a resource outside the P-3-owned hermetic provider", externalProviderPath); assertAborted(a, "p9-external-provider-resource", "external provider resource");
  const changedBundlePath = "MyBrain/verify/e2e-comparison/p9-changed-provider-bundle.json"; resetUnimplemented(a); put(a, changedBundlePath, comparison(a, "baseline", "p9-changed-provider-bundle", commit, tree)); run(a, "pair-begin", changedBundlePath); run(a, "pair-preflight", changedBundlePath, "MyBrain/verify/e2e-frozen/manifest.json"); closeState(a, changedBundlePath); put(a, "src/main.html", "<!doctype html><html><head><title>changed</title></head><body>changed after close</body></html>\n"); fails(a, "Comparison final file hash does not match the current file: src/main.html", "report", "MyBrain/verify/e2e-out/changed-provider-bundle.json", changedBundlePath); assertAborted(a, "p9-changed-provider-bundle", "changed provider bundle"); resetUnimplemented(a);
  const contractMutationPath = "MyBrain/verify/e2e-comparison/p8-contract-mutation.json"; resetUnimplemented(a); put(a, contractMutationPath, comparison(a, "baseline", "p8-contract-mutation", commit, tree)); run(a, "pair-begin", contractMutationPath); run(a, "pair-preflight", contractMutationPath, "MyBrain/verify/e2e-frozen/manifest.json"); closeState(a); const contractMutation = read(a, contractMutationPath); contractMutation.run.release.reason = "This changed after pair-preflight and must invalidate the pair."; put(a, contractMutationPath, contractMutation); fails(a, "missing a matching baseline pair-preflight record", "report", "MyBrain/verify/e2e-out/contract-mutation.json", contractMutationPath); assertAborted(a, "p8-contract-mutation", "mutated condition contract");
  const originalGitignore = readFileSync(join(a, ".gitignore"), "utf8"); resetUnimplemented(a); writeFileSync(join(a, ".gitignore"), `${originalGitignore}ignored-runtime.css\n`, "utf8"); put(a, "ignored-runtime.css", ".p3-hero { color: #000; }\n"); put(a, "MyBrain/verify/e2e-comparison/ignored-runtime.json", comparison(a, "baseline", "p8-ignored-runtime", commit, tree)); fails(a, "ignored runtime artifact", "pair-begin", "MyBrain/verify/e2e-comparison/ignored-runtime.json"); rmSync(join(a, "ignored-runtime.css"), { force: true }); writeFileSync(join(a, ".gitignore"), originalGitignore, "utf8");
  const outsideScopePath = "MyBrain/verify/e2e-comparison/p8-outside-scope.json"; resetUnimplemented(a); put(a, outsideScopePath, comparison(a, "baseline", "p8-outside-scope", commit, tree)); run(a, "pair-begin", outsideScopePath); run(a, "pair-preflight", outsideScopePath, "MyBrain/verify/e2e-frozen/manifest.json"); closeState(a); put(a, "src/outside.css", ".outside { color: #000; }\n"); fails(a, "final Git change scope and frozen change targets", "report", "MyBrain/verify/e2e-out/outside-scope.json", outsideScopePath); rmSync(join(a, "src/outside.css"), { force: true }); resetUnimplemented(a);
  const nodeOptionsPath = "MyBrain/verify/e2e-comparison/p8-node-path.json"; put(a, nodeOptionsPath, comparison(a, "baseline", "p8-node-path", commit, tree)); failsWithEnv(a, "rejects ambient NODE_PATH", { NODE_PATH: "." }, "pair-begin", nodeOptionsPath); assertAborted(a, "p8-node-path", "ambient Node loader path");
  put(a, "MyBrain/verify/e2e-frozen/node-injection.cjs", "globalThis.__p3Injected = true;\n"); const nodeOptionsInjectionPath = "MyBrain/verify/e2e-comparison/p8-node-options.json"; put(a, nodeOptionsInjectionPath, comparison(a, "baseline", "p8-node-options", commit, tree)); failsWithEnv(a, "rejects ambient NODE_OPTIONS", { NODE_OPTIONS: "--require ./MyBrain/verify/e2e-frozen/node-injection.cjs" }, "pair-begin", nodeOptionsInjectionPath); assertAborted(a, "p8-node-options", "ambient Node options");
  const nodeExecArgvPath = "MyBrain/verify/e2e-comparison/p9-node-exec-argv.json"; put(a, nodeExecArgvPath, comparison(a, "baseline", "p9-node-exec-argv", commit, tree)); failsWithNodeArgs(a, "requires a bare Node process with no execArgv", ["--require", "./MyBrain/verify/e2e-frozen/node-injection.cjs"], "pair-begin", nodeExecArgvPath); assertAborted(a, "p9-node-exec-argv", "non-empty Node execArgv");
  const wrongUrlPath = "MyBrain/verify/e2e-comparison/p8-wrong-batch-url.json"; resetUnimplemented(a); put(a, wrongUrlPath, comparison(a, "baseline", "p8-wrong-batch-url", commit, tree)); run(a, "pair-begin", wrongUrlPath); run(a, "pair-preflight", wrongUrlPath, "MyBrain/verify/e2e-frozen/manifest.json"); closeState(a); const wrongUrlState = read(a, ".figma-gate/active.json"); const wrongUrlSummaryPath = "MyBrain/verify/e2e-out/hero-batch.json"; const wrongUrlSummary = read(a, wrongUrlSummaryPath); wrongUrlSummary.url = "https://wrong.example.test/"; put(a, wrongUrlSummaryPath, wrongUrlSummary); wrongUrlState.checkpoints.hero.batchEvidence.batchSummarySha256 = sha(a, wrongUrlSummaryPath); put(a, ".figma-gate/active.json", wrongUrlState); fails(a, "recorded PASS CDP measurement for the frozen verifyUrl", "report", "MyBrain/verify/e2e-out/wrong-batch-url.json", wrongUrlPath);
  const duplicateBatchPath = "MyBrain/verify/e2e-comparison/p8-duplicate-batch.json"; resetUnimplemented(a); put(a, duplicateBatchPath, comparison(a, "baseline", "p8-duplicate-batch", commit, tree)); run(a, "pair-begin", duplicateBatchPath); run(a, "pair-preflight", duplicateBatchPath, "MyBrain/verify/e2e-frozen/manifest.json"); closeState(a); const duplicateState = read(a, ".figma-gate/active.json"); duplicateState.checkpoints.cards.batchEvidence = { ...duplicateState.checkpoints.hero.batchEvidence }; put(a, ".figma-gate/active.json", duplicateState); fails(a, "reuses a browser batch summary", "report", "MyBrain/verify/e2e-out/duplicate-batch.json", duplicateBatchPath); resetUnimplemented(a);
  const fixedMutex = join(a, ".git", "figma-p3-comparison-reservation-mutex"); mkdirSync(fixedMutex); const busyReservationPath = "MyBrain/verify/e2e-comparison/p8-busy-reservation.json"; put(a, busyReservationPath, comparison(a, "baseline", "p8-busy-reservation", commit, tree)); fails(a, "reservation is busy or stale", "pair-begin", busyReservationPath); if (existsSync(join(a, ".git", "figma-p3-comparison-pair-locks", createHash("sha256").update("p8-busy-reservation").digest("hex") + ".json"))) throw new Error("busy reservation created a permanent pair lock"); rmSync(fixedMutex, { recursive: true, force: true });
  const contractPathCollision = comparison(a, "baseline", "p8-contract-path-collision", commit, tree); put(a, "MyBrain/verify/e2e-comparison/p4-positive.json", contractPathCollision); resetUnimplemented(a); fails(a, "Comparison contract path mybrain/verify/e2e-comparison/p4-positive.json is already reserved and cannot be reused", "pair-begin", "MyBrain/verify/e2e-comparison/p4-positive.json"); put(a, "MyBrain/verify/e2e-comparison/p4-positive.json", comparison(a, "baseline", "p4-positive", commit, tree));

  resetUnimplemented(a); put(a, "MyBrain/verify/e2e-frozen/decoy-gate-browser-batch.mjs", "// decoy measurement\n"); const decoyEvaluator = comparison(a, "baseline", "p7-decoy-evaluator", commit, tree); decoyEvaluator.shared.evaluator.gateBrowserBatch = evidence(a, "MyBrain/verify/e2e-frozen/decoy-gate-browser-batch.mjs"); put(a, "MyBrain/verify/e2e-comparison/decoy-evaluator.json", decoyEvaluator); fails(a, "Comparison gateBrowserBatch must use the canonical installed artifact MyBrain/verify/gate-browser-batch.mjs", "pair-begin", "MyBrain/verify/e2e-comparison/decoy-evaluator.json");
  const baselineRecord = read(a, evaluatorBaselinePath); baselineRecord.artifacts.find((artifact) => artifact.key === "lintUnits").sha256 = "0".repeat(64); put(a, evaluatorBaselinePath, baselineRecord); put(a, "MyBrain/verify/e2e-comparison/bad-evaluator-baseline.json", comparison(a, "baseline", "p8-bad-evaluator-baseline", commit, tree)); fails(a, "Comparison evaluator baseline record artifacts differ from the frozen canonical evaluators", "pair-begin", "MyBrain/verify/e2e-comparison/bad-evaluator-baseline.json"); archiveAndApprovals(a, commit, tree);
  const originalPolicy = readFileSync(join(a, evaluatorPaths.loopLearningPolicy), "utf8"); put(a, evaluatorPaths.loopLearningPolicy, { version: 1, policy: "changed" }); put(a, "MyBrain/verify/e2e-comparison/policy-root-change.json", comparison(a, "baseline", "p8-policy-root-change", commit, tree)); fails(a, "Comparison evaluator baseline record artifacts differ from the frozen canonical evaluators", "pair-begin", "MyBrain/verify/e2e-comparison/policy-root-change.json"); writeFileSync(join(a, evaluatorPaths.loopLearningPolicy), originalPolicy, "utf8");
  const originalHelper = readFileSync(join(a, "MyBrain/verify/gate-helper.mjs"), "utf8"); const helperBeforePath = "MyBrain/verify/e2e-comparison/helper-before-preflight.json"; put(a, helperBeforePath, comparison(a, "baseline", "p8-helper-before-preflight", commit, tree)); const helperBeforeContract = read(a, helperBeforePath); const helperBeforeJ = read(a, helperBeforeContract.shared.ownerDecisionJ.path); helperBeforeJ.evaluatorExecutionBundleSha256 = "0".repeat(64); put(a, helperBeforeContract.shared.ownerDecisionJ.path, helperBeforeJ); helperBeforeContract.shared.ownerDecisionJ.sha256 = sha(a, helperBeforeContract.shared.ownerDecisionJ.path); put(a, helperBeforePath, helperBeforeContract); fails(a, "Owner decision J record evaluator execution bundle differs from the frozen canonical evaluators", "pair-begin", helperBeforePath);
  put(a, "MyBrain/verify/gate-helper.mjs", "export const helper = eval(\"true\");\n"); put(a, "MyBrain/verify/e2e-comparison/dynamic-evaluator-loader.json", comparison(a, "baseline", "p9-dynamic-evaluator-loader", commit, tree)); fails(a, "uses forbidden eval", "pair-begin", "MyBrain/verify/e2e-comparison/dynamic-evaluator-loader.json"); writeFileSync(join(a, "MyBrain/verify/gate-helper.mjs"), originalHelper, "utf8"); archiveAndApprovals(a, commit, tree);
  put(a, "MyBrain/verify/e2e-comparison/helper-after-begin.json", comparison(a, "baseline", "p8-helper-after-begin", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", "MyBrain/verify/e2e-comparison/helper-after-begin.json"); put(a, "MyBrain/verify/gate-helper.mjs", "export const helper = false;\n"); rmSync(join(a, ".figma-gate"), { recursive: true, force: true }); fails(a, "Comparison evaluator baseline record execution bundle differs from the frozen canonical evaluators", "pair-preflight", "MyBrain/verify/e2e-comparison/helper-after-begin.json", "MyBrain/verify/e2e-frozen/manifest.json"); if (existsSync(join(a, ".figma-gate/active.json"))) throw new Error("changed evaluator reached figma-gate preflight"); writeFileSync(join(a, "MyBrain/verify/gate-helper.mjs"), originalHelper, "utf8");
  const originalGate = readFileSync(join(a, evaluatorPaths.figmaGate), "utf8"); put(a, evaluatorPaths.figmaGate, `${originalGate}\nconst unlistedRuntime = "MyBrain/verify/unfrozen-runtime.mjs";\n`); put(a, "MyBrain/verify/e2e-comparison/unfrozen-runtime.json", comparison(a, "baseline", "p8-unfrozen-runtime", commit, tree)); fails(a, "Comparison figma-gate runtime artifact MyBrain/verify/unfrozen-runtime.mjs has no canonical evaluator declaration", "pair-begin", "MyBrain/verify/e2e-comparison/unfrozen-runtime.json"); writeFileSync(join(a, evaluatorPaths.figmaGate), originalGate, "utf8");
  put(a, "frozen/hidden.css", ".p3-hero { color: #000; }\n"); put(a, "MyBrain/verify/e2e-comparison/frozen-source.json", comparison(a, "baseline", "p8-frozen-source", commit, tree)); fails(a, "worktree already renders hero selector .p3-hero: frozen/hidden.css", "pair-begin", "MyBrain/verify/e2e-comparison/frozen-source.json"); rmSync(join(a, "frozen"), { recursive: true, force: true });
  const wrongRootsPath = "MyBrain/verify/e2e-comparison/wrong-evaluator-roots.json"; put(a, wrongRootsPath, comparison(a, "baseline", "p7-wrong-evaluator-roots", commit, tree)); const wrongRootsContract = read(a, wrongRootsPath); const wrongRootsJ = read(a, wrongRootsContract.shared.ownerDecisionJ.path); wrongRootsJ.evaluatorRootsSha256 = "0".repeat(64); put(a, wrongRootsContract.shared.ownerDecisionJ.path, wrongRootsJ); wrongRootsContract.shared.ownerDecisionJ.sha256 = sha(a, wrongRootsContract.shared.ownerDecisionJ.path); put(a, wrongRootsPath, wrongRootsContract); fails(a, "Owner decision J record evaluator roots differ from the frozen canonical evaluators", "pair-begin", wrongRootsPath);
  const wrongDecisionInputPath = "MyBrain/verify/e2e-comparison/wrong-decision-input.json"; put(a, wrongDecisionInputPath, comparison(a, "baseline", "p9-wrong-decision-input", commit, tree)); const wrongDecisionInputContract = read(a, wrongDecisionInputPath); const wrongDecisionInputJ = read(a, wrongDecisionInputContract.shared.ownerDecisionJ.path); wrongDecisionInputJ.comparisonInputBundleSha256 = "0".repeat(64); put(a, wrongDecisionInputContract.shared.ownerDecisionJ.path, wrongDecisionInputJ); wrongDecisionInputContract.shared.ownerDecisionJ.sha256 = sha(a, wrongDecisionInputContract.shared.ownerDecisionJ.path); put(a, wrongDecisionInputPath, wrongDecisionInputContract); fails(a, "Owner decision J record comparison input bundle differs from the frozen P-3 input", "pair-begin", wrongDecisionInputPath);
  const originalMetadata = readFileSync(join(a, "MyBrain/verify/e2e-frozen/metadata.json"), "utf8"); const decisionInputPath = "MyBrain/verify/e2e-comparison/decision-input-bundle.json"; put(a, decisionInputPath, comparison(a, "baseline", "p8-decision-input-bundle", commit, tree)); put(a, "MyBrain/verify/e2e-frozen/metadata.json", { fileKey: "figma-e2e", rootNodeId: "1:2", changed: true }); fails(a, "Comparison contract shared.figma.metadata SHA-256 mismatch", "pair-begin", decisionInputPath); writeFileSync(join(a, "MyBrain/verify/e2e-frozen/metadata.json"), originalMetadata, "utf8"); archiveAndApprovals(a, commit, tree);
  const pendingJPath = "MyBrain/verify/e2e-comparison/unapproved-j.json"; put(a, pendingJPath, comparison(a, "baseline", "p5-unapproved-j", commit, tree)); const pendingContract = read(a, pendingJPath); const pendingJ = read(a, pendingContract.shared.ownerDecisionJ.path); pendingJ.status = "pending"; put(a, pendingContract.shared.ownerDecisionJ.path, pendingJ); pendingContract.shared.ownerDecisionJ.sha256 = sha(a, pendingContract.shared.ownerDecisionJ.path); put(a, pendingJPath, pendingContract); fails(a, "requires an owner-approved decision J record", "pair-begin", pendingJPath); assertAborted(a, "p5-unapproved-j", "unapproved decision J"); put(a, "MyBrain/verify/e2e-comparison/unapproved-j-retry.json", comparison(a, "baseline", "p5-unapproved-j", commit, tree)); fails(a, "cannot be reused", "pair-begin", "MyBrain/verify/e2e-comparison/unapproved-j-retry.json");
  put(a, "src/main.html", "<!doctype html><main class=\"p3-hero\">implemented</main>\n"); put(a, "MyBrain/verify/e2e-comparison/implemented-source.json", comparison(a, "baseline", "p6-implemented-worktree", commit, tree)); fails(a, "worktree already contains implementation target src/main.html", "pair-begin", "MyBrain/verify/e2e-comparison/implemented-source.json"); resetUnimplemented(a);
  put(a, "MyBrain/verify/e2e-comparison/p5-current-approval.json", comparison(a, "baseline", "p5-current-approval", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", "MyBrain/verify/e2e-comparison/p5-current-approval.json"); const invalidCurrent = comparison(b, "current", "p5-current-approval", commit, tree); const invalidApproval = read(b, invalidCurrent.run.evaluatedChange.approvalRecord.path); invalidApproval.status = "pending"; put(b, invalidCurrent.run.evaluatedChange.approvalRecord.path, invalidApproval); invalidCurrent.run.evaluatedChange.approvalRecord.sha256 = sha(b, invalidCurrent.run.evaluatedChange.approvalRecord.path); put(b, "MyBrain/verify/e2e-comparison/p5-current-approval.json", invalidCurrent); resetUnimplemented(b); rmSync(join(b, ".figma-gate"), { recursive: true, force: true }); fails(b, "Current comparison change approval must be owner-approved", "pair-preflight", "MyBrain/verify/e2e-comparison/p5-current-approval.json", "MyBrain/verify/e2e-frozen/manifest.json"); if (existsSync(join(b, ".figma-gate/active.json"))) throw new Error("unapproved current change reached figma-gate preflight");
  const currentPathBaseline = "MyBrain/verify/e2e-comparison/p8-current-path.json"; const currentPathOther = "MyBrain/verify/e2e-comparison/p8-current-path-other.json"; put(a, currentPathBaseline, comparison(a, "baseline", "p8-current-path", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", currentPathBaseline); put(b, currentPathOther, comparison(b, "current", "p8-current-path", commit, tree)); resetUnimplemented(b); rmSync(join(b, ".figma-gate"), { recursive: true, force: true }); fails(b, "Comparison pair p8-current-path is reserved for mybrain/verify/e2e-comparison/p8-current-path.json, not mybrain/verify/e2e-comparison/p8-current-path-other.json", "pair-preflight", currentPathOther, "MyBrain/verify/e2e-frozen/manifest.json"); assertAborted(a, "p8-current-path", "different current contract path"); if (existsSync(join(b, ".figma-gate/active.json"))) throw new Error("different current contract path reached figma-gate preflight");

  put(a, "MyBrain/verify/e2e-comparison/plan.json", comparison(a, "baseline", "p4-frozen-plan", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", "MyBrain/verify/e2e-comparison/plan.json"); run(a, "pair-preflight", "MyBrain/verify/e2e-comparison/plan.json", "MyBrain/verify/e2e-frozen/manifest.json"); closeState(a); const changed = read(a, ".figma-gate/active.json"); changed.benchmark.plan = ["hero"]; changed.checkpoints = { hero: changed.checkpoints.hero }; changed.changeTargets = ["src/main.css"]; put(a, ".figma-gate/active.json", changed); fails(a, "benchmark plan and frozen gate manifest", "report", "MyBrain/verify/e2e-out/plan.json", "MyBrain/verify/e2e-comparison/plan.json");

  put(a, "MyBrain/verify/e2e-comparison/package.json", comparison(a, "baseline", "p4-package-lock", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", "MyBrain/verify/e2e-comparison/package.json"); run(a, "pair-preflight", "MyBrain/verify/e2e-comparison/package.json", "MyBrain/verify/e2e-frozen/manifest.json"); closeState(a); const lock = readFileSync(join(a, "package-lock.json"), "utf8"); put(a, "package-lock.json", "{\"changed\":true}\n"); fails(a, "packageLock SHA-256 mismatch", "report", "MyBrain/verify/e2e-out/package.json", "MyBrain/verify/e2e-comparison/package.json"); writeFileSync(join(a, "package-lock.json"), lock, "utf8");
  const linkedLock = read(a, "package-lock.json"); linkedLock.packages["node_modules/e2e-bare"].version = "9.9.9"; put(a, "package-lock.json", linkedLock); put(a, "MyBrain/verify/e2e-comparison/lock-link.json", comparison(a, "baseline", "p5-lock-link", commit, tree)); fails(a, "package lock version does not match resolved e2e-bare", "pair-begin", "MyBrain/verify/e2e-comparison/lock-link.json"); put(a, "package-lock.json", lock);
  put(a, "MyBrain/verify/e2e-comparison/bare.json", comparison(a, "baseline", "p4-bare-module", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", "MyBrain/verify/e2e-comparison/bare.json"); run(a, "pair-preflight", "MyBrain/verify/e2e-comparison/bare.json", "MyBrain/verify/e2e-frozen/manifest.json"); closeState(a); const moduleSource = readFileSync(join(a, "node_modules/e2e-bare/index.js"), "utf8"); put(a, "node_modules/e2e-bare/index.js", "export const bare = false;\n"); fails(a, "Comparison evaluator baseline record execution bundle differs from the frozen canonical evaluators", "report", "MyBrain/verify/e2e-out/bare.json", "MyBrain/verify/e2e-comparison/bare.json"); writeFileSync(join(a, "node_modules/e2e-bare/index.js"), moduleSource, "utf8");

  put(a, "MyBrain/verify/e2e-comparison/bare-helper.json", comparison(a, "baseline", "p5-bare-helper", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", "MyBrain/verify/e2e-comparison/bare-helper.json"); run(a, "pair-preflight", "MyBrain/verify/e2e-comparison/bare-helper.json", "MyBrain/verify/e2e-frozen/manifest.json"); closeState(a); const helperSource = readFileSync(join(a, "node_modules/e2e-bare/helper.js"), "utf8"); put(a, "node_modules/e2e-bare/helper.js", "export const helper = false;\n"); fails(a, "Comparison evaluator baseline record execution bundle differs from the frozen canonical evaluators", "report", "MyBrain/verify/e2e-out/bare-helper.json", "MyBrain/verify/e2e-comparison/bare-helper.json"); writeFileSync(join(a, "node_modules/e2e-bare/helper.js"), helperSource, "utf8");

  put(a, "MyBrain/verify/e2e-comparison/conditional-import-helper.json", comparison(a, "baseline", "p8-conditional-import-helper", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", "MyBrain/verify/e2e-comparison/conditional-import-helper.json"); run(a, "pair-preflight", "MyBrain/verify/e2e-comparison/conditional-import-helper.json", "MyBrain/verify/e2e-frozen/manifest.json"); closeState(a); const conditionalImportHelper = readFileSync(join(a, "node_modules/e2e-conditional/import-helper.js"), "utf8"); put(a, "node_modules/e2e-conditional/import-helper.js", "export const importedHelper = false;\n"); fails(a, "Comparison evaluator baseline record execution bundle differs from the frozen canonical evaluators", "report", "MyBrain/verify/e2e-out/conditional-import-helper.json", "MyBrain/verify/e2e-comparison/conditional-import-helper.json"); writeFileSync(join(a, "node_modules/e2e-conditional/import-helper.js"), conditionalImportHelper, "utf8");

  put(a, "MyBrain/verify/e2e-comparison/conditional-require-helper.json", comparison(a, "baseline", "p8-conditional-require-helper", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", "MyBrain/verify/e2e-comparison/conditional-require-helper.json"); run(a, "pair-preflight", "MyBrain/verify/e2e-comparison/conditional-require-helper.json", "MyBrain/verify/e2e-frozen/manifest.json"); closeState(a); const conditionalRequireHelper = readFileSync(join(a, "node_modules/e2e-conditional/require-helper.cjs"), "utf8"); put(a, "node_modules/e2e-conditional/require-helper.cjs", "module.exports = false;\n"); fails(a, "Comparison evaluator baseline record execution bundle differs from the frozen canonical evaluators", "report", "MyBrain/verify/e2e-out/conditional-require-helper.json", "MyBrain/verify/e2e-comparison/conditional-require-helper.json"); writeFileSync(join(a, "node_modules/e2e-conditional/require-helper.cjs"), conditionalRequireHelper, "utf8");

  put(a, "MyBrain/verify/e2e-comparison/cjs-helper.json", comparison(a, "baseline", "p8-cjs-helper", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", "MyBrain/verify/e2e-comparison/cjs-helper.json"); run(a, "pair-preflight", "MyBrain/verify/e2e-comparison/cjs-helper.json", "MyBrain/verify/e2e-frozen/manifest.json"); closeState(a); const cjsHelperSource = readFileSync(join(a, "node_modules/e2e-cjs/helper.cjs"), "utf8"); put(a, "node_modules/e2e-cjs/helper.cjs", "module.exports = false;\n"); fails(a, "Comparison evaluator baseline record execution bundle differs from the frozen canonical evaluators", "report", "MyBrain/verify/e2e-out/cjs-helper.json", "MyBrain/verify/e2e-comparison/cjs-helper.json"); writeFileSync(join(a, "node_modules/e2e-cjs/helper.cjs"), cjsHelperSource, "utf8");
  const cjsEntrySource = readFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), "utf8"); put(a, "node_modules/e2e-cjs/index.cjs", "const helperPath = \"./helper.cjs\"; const helper = require(helperPath); module.exports = helper;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-dynamic.json", comparison(a, "baseline", "p8-cjs-dynamic", commit, tree)); fails(a, "has a dynamic import or require", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-dynamic.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const invoke = require; const helper = invoke(\"./helper.cjs\"); module.exports = helper;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-alias.json", comparison(a, "baseline", "p8-cjs-alias", commit, tree)); fails(a, "aliases or indirectly references require", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-alias.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const { require: invoke } = module; const helper = invoke(\"./helper.cjs\"); module.exports = helper;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-destructure.json", comparison(a, "baseline", "p8-cjs-destructure", commit, tree)); fails(a, "static execution bundle evidence requires a direct literal module call", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-destructure.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const invoke = module[\"require\"]; const helper = invoke(\"./helper.cjs\"); module.exports = helper;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-bracket.json", comparison(a, "baseline", "p8-cjs-bracket", commit, tree)); fails(a, "uses forbidden bracket-based runtime loader lookup", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-bracket.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const invoke = require.bind(null); const helper = invoke(\"./helper.cjs\"); module.exports = helper;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-bind.json", comparison(a, "baseline", "p8-cjs-bind", commit, tree)); fails(a, "static execution bundle evidence requires a direct literal module call", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-bind.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const load = module.constructor._load; const helper = load(\"./helper.cjs\"); module.exports = helper;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-module-load.json", comparison(a, "baseline", "p8-cjs-module-load", commit, tree)); fails(a, "uses forbidden Module runtime loader", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-module-load.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const helper = `${require(\"./helper.cjs\")}`; module.exports = helper;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-template-loader.json", comparison(a, "baseline", "p8-cjs-template-loader", commit, tree)); fails(a, "template-literal module loader", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-template-loader.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const ghost = require(\"node:not-real\"); module.exports = ghost;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-unknown-node.json", comparison(a, "baseline", "p8-cjs-unknown-node", commit, tree)); fails(a, "unknown Node builtin import: node:not-real", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-unknown-node.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const helper = require(\"./helper.cjs\")[\"value\"]; module.exports = helper;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-result-bracket.json", comparison(a, "baseline", "p10-cjs-result-bracket", commit, tree)); fails(a, "indirect require result", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-result-bracket.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const helper = require(\"./helper.cjs\")?.value; module.exports = helper;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-result-optional.json", comparison(a, "baseline", "p10-cjs-result-optional", commit, tree)); fails(a, "indirect require result", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-result-optional.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const helper = require(\"./helper.cjs\").value.next; module.exports = helper;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-result-chain.json", comparison(a, "baseline", "p10-cjs-result-chain", commit, tree)); fails(a, "indirect require result", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-result-chain.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const helper = module.require(\"./helper.cjs\").value; module.exports = helper;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-module-result-property.json", comparison(a, "baseline", "p10-cjs-module-result-property", commit, tree)); fails(a, "indirect require result", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-module-result-property.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const helper = require(\"assert\").ok(true); module.exports = helper;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-result-invocation.json", comparison(a, "baseline", "p10-cjs-result-invocation", commit, tree)); fails(a, "indirect require result", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-result-invocation.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const factory = require(\"./helper.cjs\").constructor; module.exports = factory;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-result-constructor.json", comparison(a, "baseline", "p10-cjs-result-constructor", commit, tree)); fails(a, "indirect require result", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-result-constructor.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const loader = require(\"process\").getBuiltinModule; module.exports = loader;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-process-get-builtin-module.json", comparison(a, "baseline", "p10-cjs-process-get-builtin-module", commit, tree)); fails(a, "forbidden direct runtime loader property process.getBuiltinModule", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-process-get-builtin-module.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const loader = require(\"process\").binding; module.exports = loader;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-process-binding.json", comparison(a, "baseline", "p10-cjs-process-binding", commit, tree)); fails(a, "forbidden direct runtime loader property process.binding", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-process-binding.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const loader = require(\"process\").mainModule; module.exports = loader;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-process-main-module.json", comparison(a, "baseline", "p10-cjs-process-main-module", commit, tree)); fails(a, "forbidden direct runtime loader property process.mainModule", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-process-main-module.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const loader = require(\"process\").dlopen; module.exports = loader;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-process-dlopen.json", comparison(a, "baseline", "p10-cjs-process-dlopen", commit, tree)); fails(a, "forbidden direct runtime loader property process.dlopen", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-process-dlopen.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const loader = require(\"node:process\").getBuiltinModule; module.exports = loader;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-node-process-get-builtin-module.json", comparison(a, "baseline", "p10-cjs-node-process-get-builtin-module", commit, tree)); fails(a, "forbidden direct runtime loader property node:process.getBuiltinModule", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-node-process-get-builtin-module.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const loader = require(\"module\").createRequire; module.exports = loader;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-module-create-require.json", comparison(a, "baseline", "p10-cjs-module-create-require", commit, tree)); fails(a, "forbidden direct runtime loader property module.createRequire", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-module-create-require.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/index.cjs", "const loader = require(\"node:module\").createRequire; module.exports = loader;\n"); put(a, "MyBrain/verify/e2e-comparison/cjs-node-module-create-require.json", comparison(a, "baseline", "p10-cjs-node-module-create-require", commit, tree)); fails(a, "forbidden direct runtime loader property node:module.createRequire", "pair-begin", "MyBrain/verify/e2e-comparison/cjs-node-module-create-require.json"); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8");
  put(a, "node_modules/e2e-cjs/helper-plain.cjs", "module.exports = true;\n"); put(a, "node_modules/e2e-cjs/helper-module.cjs", "module.exports = true;\n"); put(a, "node_modules/e2e-cjs/helper-resolve.cjs", "module.exports = true;\n"); put(a, "node_modules/e2e-cjs/index.cjs", "const plain = require(\"./helper-plain.cjs\"); const throughModule = module.require(\"./helper-module.cjs\"); const resolved = require.resolve(\"./helper-resolve.cjs\"); const fs = require(\"fs\"); module.exports = Boolean(plain && throughModule && resolved && fs);\n"); archiveAndApprovals(a, commit, tree); put(a, "MyBrain/verify/e2e-comparison/cjs-static-syntax.json", comparison(a, "baseline", "p8-cjs-static-syntax", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", "MyBrain/verify/e2e-comparison/cjs-static-syntax.json"); run(a, "pair-abort", "MyBrain/verify/e2e-comparison/cjs-static-syntax.json", "E2E static CJS parser cleanup."); writeFileSync(join(a, "node_modules/e2e-cjs/index.cjs"), cjsEntrySource, "utf8"); rmSync(join(a, "node_modules/e2e-cjs/helper-plain.cjs"), { force: true }); rmSync(join(a, "node_modules/e2e-cjs/helper-module.cjs"), { force: true }); rmSync(join(a, "node_modules/e2e-cjs/helper-resolve.cjs"), { force: true }); archiveAndApprovals(a, commit, tree);

  put(a, "MyBrain/verify/e2e-comparison/chrome.json", comparison(a, "baseline", "p4-chrome-evidence", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", "MyBrain/verify/e2e-comparison/chrome.json"); run(a, "pair-preflight", "MyBrain/verify/e2e-comparison/chrome.json", "MyBrain/verify/e2e-frozen/manifest.json"); closeState(a); const stateChrome = read(a, ".figma-gate/active.json"); const heroSummary = read(a, "MyBrain/verify/e2e-out/hero-batch.json"); heroSummary.browser.revision = "wrong-revision"; put(a, "MyBrain/verify/e2e-out/hero-batch.json", heroSummary); stateChrome.checkpoints.hero.batchEvidence.batchSummarySha256 = sha(a, "MyBrain/verify/e2e-out/hero-batch.json"); put(a, ".figma-gate/active.json", stateChrome); fails(a, "Comparison Chrome fingerprint changed between final batches", "report", "MyBrain/verify/e2e-out/chrome.json", "MyBrain/verify/e2e-comparison/chrome.json");

  put(a, "MyBrain/verify/e2e-comparison/clean.json", comparison(a, "baseline", "p5-clean-room", commit, tree)); const badClean = read(a, "MyBrain/verify/e2e-comparison/clean.json"); badClean.shared.cleanRoomAuthorization.conditions[0].otherConditionArtifactsAccessible = true; put(a, "MyBrain/verify/e2e-comparison/clean.json", badClean); fails(a, "must explicitly deny access", "pair-begin", "MyBrain/verify/e2e-comparison/clean.json");

  put(a, "MyBrain/verify/e2e-comparison/repreflight.json", comparison(a, "baseline", "p7-direct-repreflight", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", "MyBrain/verify/e2e-comparison/repreflight.json"); run(a, "pair-preflight", "MyBrain/verify/e2e-comparison/repreflight.json", "MyBrain/verify/e2e-frozen/manifest.json"); execFileSync(process.execPath, [join(a, evaluatorPaths.figmaGate), "preflight", "MyBrain/verify/e2e-frozen/manifest.json", "--implementation-actor", "implementation-baseline", "--implementation-context-id", "implementation-baseline"], { cwd: a, encoding: "utf8" }); closeState(a); put(a, "MyBrain/verify/e2e-comparison/repreflight.json", comparison(a, "baseline", "p7-direct-repreflight", commit, tree)); fails(a, "Active Figma gate preflight instance differs from the recorded pair-preflight", "report", "MyBrain/verify/e2e-out/repreflight.json", "MyBrain/verify/e2e-comparison/repreflight.json");
  put(a, "MyBrain/verify/e2e-comparison/malformed.json", comparison(a, "baseline", "p4-malformed-report", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", "MyBrain/verify/e2e-comparison/malformed.json"); put(a, "MyBrain/verify/e2e-out/malformed.json", "{ not-json\n"); fails(a, "Baseline benchmark is not valid JSON", "compare", "MyBrain/verify/e2e-out/malformed.json", "MyBrain/verify/e2e-out/malformed.json", "MyBrain/verify/e2e-comparison/malformed.json"); assertAborted(a, "p4-malformed-report", "early compare failure");
  const malformedPreflightPath = "MyBrain/verify/e2e-comparison/malformed-contract-preflight.json"; put(a, malformedPreflightPath, comparison(a, "baseline", "p8-malformed-contract-preflight", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", malformedPreflightPath); put(a, malformedPreflightPath, "{ not-json\n"); fails(a, "Comparison contract is not valid JSON", "pair-preflight", malformedPreflightPath, "MyBrain/verify/e2e-frozen/manifest.json"); assertAborted(a, "p8-malformed-contract-preflight", "malformed pair-preflight contract"); put(a, malformedPreflightPath, comparison(a, "baseline", "p8-malformed-contract-preflight", commit, tree)); fails(a, "already recorded and cannot be reused", "pair-begin", malformedPreflightPath);
  const malformedReportPath = "MyBrain/verify/e2e-comparison/malformed-contract-report.json"; put(a, malformedReportPath, comparison(a, "baseline", "p8-malformed-contract-report", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", malformedReportPath); put(a, malformedReportPath, "{ not-json\n"); fails(a, "Comparison contract is not valid JSON", "report", "MyBrain/verify/e2e-out/malformed-contract-report.json", malformedReportPath); assertAborted(a, "p8-malformed-contract-report", "malformed report contract"); put(a, malformedReportPath, comparison(a, "baseline", "p8-malformed-contract-report", commit, tree)); fails(a, "already recorded and cannot be reused", "pair-begin", malformedReportPath);
  const malformedComparePath = "MyBrain/verify/e2e-comparison/malformed-contract-compare.json"; put(a, malformedComparePath, comparison(a, "baseline", "p8-malformed-contract-compare", commit, tree)); resetUnimplemented(a); run(a, "pair-begin", malformedComparePath); put(a, malformedComparePath, "{ not-json\n"); fails(a, "Comparison contract is not valid JSON", "compare", "MyBrain/verify/e2e-out/missing-baseline.json", "MyBrain/verify/e2e-out/missing-current.json", malformedComparePath); assertAborted(a, "p8-malformed-contract-compare", "malformed compare contract"); put(a, malformedComparePath, comparison(a, "baseline", "p8-malformed-contract-compare", commit, tree)); fails(a, "already recorded and cannot be reused", "pair-begin", malformedComparePath);
  resetUnimplemented(a); put(a, "src/main.html", "<!doctype html><main class=\"p3-hero\">implemented</main>\n"); git(a, "add", "src/main.html"); git(a, "commit", "-m", "implemented target source"); const implementedCommit = git(a, "rev-parse", "HEAD"); const implementedTree = git(a, "rev-parse", "HEAD^{tree}"); archiveAndApprovals(a, implementedCommit, implementedTree); put(a, "MyBrain/verify/e2e-comparison/source-tree-implemented.json", comparison(a, "baseline", "p5-source-tree-implemented", implementedCommit, implementedTree)); fails(a, "source snapshot already contains implementation target src/main.html", "pair-begin", "MyBrain/verify/e2e-comparison/source-tree-implemented.json");
  put(a, "MyBrain/verify/e2e-comparison/ledger-write.json", comparison(a, "baseline", "p6-ledger-write", commit, tree)); resetUnimplemented(a);
  failsWithEnv(a, "Comparison ledger append test injection", { FIGMA_P3_TEST_FAIL_LEDGER_APPEND: "1" }, "pair-begin", "MyBrain/verify/e2e-comparison/ledger-write.json");
  fails(a, "already reserved and cannot be reused", "pair-begin", "MyBrain/verify/e2e-comparison/ledger-write.json");
  git(a, "rm", "-f", "--cached", "src/main.html"); put(a, "frozen/committed.css", ".p3-hero { color: #000; }\n"); git(a, "add", "frozen/committed.css"); git(a, "commit", "-m", "frozen source remains implemented"); const frozenCommit = git(a, "rev-parse", "HEAD"); const frozenTree = git(a, "rev-parse", "HEAD^{tree}"); archiveAndApprovals(a, frozenCommit, frozenTree); put(a, "MyBrain/verify/e2e-comparison/frozen-source-tree.json", comparison(a, "baseline", "p8-frozen-source-tree", frozenCommit, frozenTree)); fails(a, "source snapshot already renders hero selector .p3-hero", "pair-begin", "MyBrain/verify/e2e-comparison/frozen-source-tree.json");
  console.log("fidelity-benchmark E2E PASS");
} finally { try { if (existsSync(b)) git(a, "worktree", "remove", "--force", b); } catch {} rmSync(a, { recursive: true, force: true }); rmSync(b, { recursive: true, force: true }); }
