# 独立批評 2026-08-21 — Codex変更 `0c8c866`「CodexクラウドでもFigmaワークフロー制約を強制」

- 実施: クラウドセッション（`node tools/workflow-preflight.mjs` → `cloud-restricted`、signals=`CLAUDE_CODE_REMOTE=true`）
- 範囲: `WORKFLOW.md`「クラウドセッションでの実行範囲」が許可する**独立レビュー（批評のみ）と静的整合確認**に限定
- 正本（`rules/` `spec/` `templates/` `tools/`）は一切変更していない
- 対象: `AGENTS.md` / `CLAUDE.md` / `README.md` / `WORKFLOW.md` / `tools/workflow-preflight.mjs` / `tools/workflow-preflight.e2e.mjs`
- 前提文書: `AUDIT-2026-08-21-rule-adherence.md`（本リポジトリ、`00c2742` でマージ済み）

## 結論

**変更の方向は正しく、実在した欠陥を1つ確実に塞いでいる。だが「Codexがルールを無視してデザインどおりに書かない」という報告された症状は、この変更では直らない。**塞いだのはクラウド判定の欠陥であり、症状が起きているのは案件ローカルである。

加えて、規定のまま運用すると**案件ローカルで着手不能になる**穴が1件（R-1）、強制力が無いまま指示だけが増えた点が1件（R-3）ある。R-1は「規則どおりにやると作業できない」形なので、放置すると規則を無視する経路をこちら側から正当化することになる。先に直すべき。

---

## 1. 実測

| 検査 | 結果 |
| --- | --- |
| `node tools/workflow-preflight.mjs`（クラウド、リポジトリ直下） | `cloud-restricted` / signals=`["CLAUDE_CODE_REMOTE=true"]` / missing=2件、**exit 0** |
| `node tools/workflow-preflight.e2e.mjs` | PASS（4アサーション） |
| `tools/figma-log-promote.e2e.mjs` / `tools/figma-scope-lock.e2e.mjs` | PASS（回帰なし） |
| リポジトリ外のcwdから `node tools/workflow-preflight.mjs` | **exit 1**（モジュール解決失敗） |
| Windows絶対パスでのmain判定 `import.meta.url === new URL('file://'+argv[1]).href` | 一致する（`C:\AI\...` は `file:///C:/AI/...` に正規化される。**バグではない**） |
| 空白を含むパスからの実行 | 正常動作 |

## 2. 妥当と認める点

- **V-1.** `CLAUDE_CODE_REMOTE` 単独判定は実在の欠陥だった（`71e4509`、こちらが入れたもの）。Codexクラウドはこの変数を持たないため、Codexは自分をローカルと誤認し、存在しない上位層を「読んだ」ことにできる状態だった。指摘は正しい。
- **V-2.** 最終的な安全弁を**環境変数ではなくファイル可読性**に置いたのは正しい設計。環境変数リストの当たり外れに関係なく、上位層を読めない環境は必ず `cloud-restricted` に落ちる。フェイルセーフの向きが合っている。
- **V-3.** `env` と `canReadPath` を注入する純関数にしたため、E2Eが実環境非依存で書けている。負の側（Claudeクラウド／Codexクラウド／上位層欠落）と正の側（両ファイル可読＝local）の4件を固定していて、テストの粒度も妥当。

## 3. 問題点

### R-1（重大・そのままでは案件で着手不能）
`WORKFLOW.md:7` は「他の調査・編集より先に**リポジトリ直下で** `node tools/workflow-preflight.mjs` を実行する。非0終了…なら着手しない」と規定した。しかし `STATE.md` の実測どおり、案件の作業ディレクトリは `C:/docker-project/rpa-technologies/...` であり `tools/` は無い。**実測でこの場合 exit 1 になる。**規定を字面どおり守れば案件では永久に着手できず、絶対パス形（`node C:\AI\figma-to-code\tools\workflow-preflight.mjs`）も「先に `cd` せよ」もどの文書にも書かれていない。

結果として現場では「各エージェントが適当に解釈する」か「無視する」になる。**守れない形で書かれた規則は、無視の口実を規則側が供給する。**監査B節（読んだことにして着手する経路が最も安い）の再生産である。

### R-2（重大・配送は未解決＝症状が直らない理由）
「最初のツール実行でpreflightを実行する」という指示は `AGENTS.md` / `CLAUDE.md` の中にある。監査A節の実測どおり、Codexが自動読込するのは **cwdとその祖先、および `~/.codex/AGENTS.md`** だけであり、`C:\AI\figma-to-code` は案件ディレクトリの祖先ではない。**届いていないファイルに1行足しても届かない。**

今回の変更が効くのは「figma-to-codeリポジトリを直接開いたセッション」だけで、症状が報告されている案件セッションには到達しない。監査の最優先提案 P-A（案件リポジトリ側に `AGENTS.md` を1枚置き祖先チェーンに乗せる／着手前ゲートだけは入口に本文で直書きする）は未着手のままである。

### R-3（中・強制力が無い）
preflightは `local` / `cloud-restricted` のどちらでも **exit 0**（実測）で、判定はstdoutにしか出ない。したがって hook・gate・CI から機械的に分岐できず、「実行したふり」も検出できない。監査B節が指摘した「規則が任意読みで、読み飛ばしを検知する仕組みが無い」構造は変わっていない。増えたのは**任意で実行する手順が1つ**である。

改善案: `--assert-local` を追加して `cloud-restricted` なら非0終了させ、`figma:gate preflight` の先頭から呼ぶ。そうすれば honor system ではなくゲートになる。

### R-4（中・用語衝突）
`preflight` は既に `npm run figma:gate -- preflight`（編集前ゲート）の名前として `rules/` 内に42箇所ある。入口2枚の「**preflight が失敗した場合は作業を開始してはいけません**」は、どちらのpreflightか区別できない。環境判定のpreflightをPASSさせたことを、spec・node map・component decision・D-012スコープロックを伴う**本来の編集前ゲートを通した**と誤認する経路ができる。これは「工程を飛ばして実装に入る」という当の症状と同じ向きの誤りである。

改善案: 文中では常に `workflow-preflight` と修飾するか、ツール名を `env-check` などに改名する。

### R-5（中・規則本文と実装の不一致／死条件）
`WORKFLOW.md:7`「非0終了、**判定不能**、または **`local` で必須上位ファイルが欠けた**場合は着手しない」。実装は (a) `判定不能` を返す経路を持たない、(b) `local` は必須2ファイルが両方可読なときにしか返らないため「`local` かつ欠落」は**発生し得ない死条件**である。規則本文に実装上ありえない条件が書かれている。

### R-6（中・偽 `local` のリスク）
判定は `accessSync(R_OK)` だけで、中身も版も見ない。監査C節の実測どおり、共通Vaultは 2026-07-29 に `C:\AI\MyBrain` → `C:\AI\vault` へ改名済みで、旧パス参照が5箇所残っている環境である。**空ファイルや旧世代のコピーが1つ置かれているだけで `local` を返し、「上位規則を読める」と誤って宣言する。**世代差を実装欠落と誤診断した 2026-07-27 の失敗と同じ地雷を踏みうる。

改善案: 見出し行や版マーカーの照合、最小バイト数の検査を足す。

### R-7（軽・未実測の主張が正本に入った）
`CODEX_CI=1` がCodexクラウドで実際に設定される根拠が、リポジトリのどこにも記録されていない。同じ `WORKFLOW.md` は「2026-08-21 実測：クラウドの既定イメージに `google-chrome` は無く…」と根拠を残す様式を持っており、その様式に反する。V-2のfallbackがあるため現時点で実害は出ないが、「クラウド判定の**正本**」と書かれた節に未検証の値が並んでいる。

改善案: 実測記録を1行足すか、環境変数を「補助シグナル」に格下げし、ファイル検査を正本と明記する。

### R-8（軽・移植性とフェイルオープン方向）
必須パスがWindows絶対パス固定のため、WSL/macOS/Linuxのローカルは常に `cloud-restricted` になる。オーナー環境はWindowsなので現時点の実害は無いが、この誤判定は**安全側ではない**。`cloud-restricted` は `spec/`・`rules/`・`tools/` の修正を許可する状態なので、「上位規則を読めるのに読まないまま正本を触れる」方向へ倒れる。上位層のルートを環境変数で上書きできるようにするのが妥当。

### R-9（軽・記録の欠落）
今回の変更は `WORKFLOW.md` 末節の「この手法自体を編集する場合」に該当するが、`STATE.md` に記録が無い（末尾は 2026-08-07）。またクラウドから正本（`WORKFLOW.md` と入口2枚）を変更しており、これは「機械的に検証できる修正」を超える。PR #1 のマージがオーナー承認に当たると解するが、その旨がリポジトリ内に残っていない。本文書が独立批評に当たるので、あわせてSTATE記録が要る。

### R-10（軽・監査指摘の積み残し）
`README.md` を編集しながら「版: 0.3.1 / 最終更新: 2026-07-18」を更新していない（監査F-4）。同ファイルの必読リストに `rules/figma-scope-lock.md` が無い点（監査F-1）も未修正。

---

## 4. 「デザインどおりに書かない」に対する効果の評価

監査が挙げた原因A〜Fのうち、今回の変更が触れたものは**無い**。

| 原因 | 状態 |
| --- | --- |
| A. 規則がCodexに届く経路が無い | 未着手（R-2） |
| B. 必読量が実行不可能 / 読み飛ばしを検知できない | 未着手。手順が1つ増えた分わずかに悪化（R-3） |
| C. ゲート配送が壊れている（旧 `C:\AI\MyBrain` 参照5箇所） | 未着手 |
| D. `unverified-figma-value` 5件が全件 `pending-review` で停止 | 未着手。**症状の本体はここ** |
| E. ループがP-3/P-11に吸われ忠実度が0件測定 | 未着手 |
| F. 静的整合の不良 | 未着手（R-10） |

Codexの変更は**クラウドセッションが上位層の欠落を自覚するための修正**であり、それ自体は正しい。だが症状が出ているのはローカルの案件セッションであって、そこには今回の変更は1バイトも届かない。

## 5. 提案（優先順）

- **P-1（R-1・R-4）** `WORKFLOW.md` と入口2枚の文言を修正する。実行形を絶対パスで明記し、`preflight` の語を `workflow-preflight` に統一する。文言修正のみで機械的に検証できる。
- **P-2（R-3）** `--assert-local` を追加し、`figma:gate preflight` の先頭から呼ぶ。honor systemをゲートに変える最小の一歩。
- **P-3（R-6）** 上位層ファイルの版マーカー照合を足し、空・旧世代のファイルで `local` にならないようにする。
- **P-4（R-5・R-7・R-9・R-10）** 死条件の削除、`CODEX_CI` の実測記録、STATE記録、README状態欄と必読リストの修正。
- **P-5** 監査の **P-A（案件側 `AGENTS.md` の設置）** と **P-C（`unverified-figma-value` 5件の処理）** は依然として最優先である。今回の変更はこの2つの代替にならない。

P-1〜P-4は正本の変更を伴うため、着手にはオーナー承認が必要。

---

## 6. 対応（2026-08-21 claude、オーナー指示「修正しろ」）

同セッションで P-1〜P-4 を適用した。適用範囲はクラウドで機械的に検証できるものに限る。

| 指摘 | 対応 |
| --- | --- |
| R-1 | `WORKFLOW.md` / 入口2枚 / `README.md` の実行形を cwd 非依存の絶対パスに統一。「非0終了、判定不能…」を「非0終了、またはJSON判定が得られない場合」に改め、案件cwdでも実行できるようにした |
| R-2 | 本リポジトリ側のみ実施。入口2枚に**着手前ゲート5項目を本文として直書き**し、「規則本文を入口へ複製しない」設計をこのゲートに限り例外にした。**案件側 `AGENTS.md` の設置はクラウドから実施できないため未了** |
| R-3 | `--assert-local` を追加。`cloud-restricted` は **exit 2**、`local` は exit 0。実プロセス起動のE2Eで終了コードを固定。`rules/figma-spec-pipeline.md` のフェーズ0チェックリストと実行ゲート節に前段として追加した |
| R-4 | 文書中の呼称を「環境判定 `workflow-preflight`」に統一し、`figma:gate preflight` とは別物で両方通す旨を `WORKFLOW.md`・入口・`README.md`・`figma-spec-pipeline.md` に明記 |
| R-5 | 死条件（`local` かつ上位ファイル欠落）と、実装が返さない「判定不能」を規則本文から削除 |
| R-6 | 判定を `accessSync` から**実読**へ変更。下限200バイトとMarkdown見出しの有無を検査し、空・プレースホルダで `local` にならないようにした。E2Eに負の3件を追加。旧世代コピーは検出できない旨を規則本文に明記 |
| R-7 | 環境変数を「補助シグナル」に格下げし、判定の正本をファイルの実読と規定。`CLAUDE_CODE_REMOTE` は実測済み、`CODEX_CI` は未実測であることを `WORKFLOW.md` に記録 |
| R-8 | `FIGMA_TO_CODE_VAULT_WORKFLOW` / `FIGMA_TO_CODE_WEB_DEVELOPMENT_WORKFLOW` で上位層パスを上書き可能にし、E2Eで固定 |
| R-9 | `STATE.md` に本件の記録を追加 |
| R-10 | `README.md` の版・最終更新を更新し、必読リストに `rules/figma-scope-lock.md` を追加 |

実測: `tools/workflow-preflight.e2e.mjs` は9群へ拡張し PASS。`tools/figma-log-promote.e2e.mjs` / `tools/figma-scope-lock.e2e.mjs` に回帰なし。`--assert-local` はこのクラウドで exit 2、既定実行は exit 0。

### 未実施（ローカルセッションが必要）

1. **案件側 `AGENTS.md` の設置**（監査P-A の本体）。本リポジトリは案件cwdの祖先ではないため、クラウドからは配送経路を作れない。**これが未了の間、今回の入口強化は案件セッションに届かない。**
2. **`figma-gate` から `--assert-local` を自動起動する配線**。案件側 `package.json` と実測が要る。現状は「チェックリストに書かれた手順」であり、機械的強制ではない。
3. 監査C（旧 `C:\AI\MyBrain` 参照5箇所）、監査D（`unverified-figma-value` 5件の滞留）、監査E（忠実度ベンチマーク0件）。いずれも別scopeであり、`WORKFLOW.md` のscope分離規定により本scopeへ混ぜていない。
